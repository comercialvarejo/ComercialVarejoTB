"""
Atualiza o index.html do Estoque Apucarana a partir da Planilha Google
("banco de dados" do estoque), publicada na web como CSV.

Requer 1 GitHub Secret:
  GSHEET_CSV_URL -> link de "Publicar na Web" da aba de estoque, em CSV
                     (Planilha Google -> Arquivo -> Compartilhar ->
                      Publicar na Web -> escolher a aba -> formato CSV)

Secrets opcionais (o script funciona sem elas, só fica sem o recurso
correspondente):
  GSHEET_VALIDADES_CSV_URL   -> botão "ver validades" de cada card
  GSHEET_PRECOS_CSV_URL      -> tabela-precos.html
  GSHEET_PEDIDOS_CSV_URL     -> pedidos-em-aberto.html
  GSHEET_PROGRAMACAO_CSV_URL -> botão "🚚 programação" de cada card do
                                 estoque (planilha de programação de
                                 cargas, cruzada pela coluna CÓDIGO PROTHEUS)

Este script pode ser disparado de duas formas (ambas já configuradas no
workflow):
  1) De hora em hora (agendado), como rede de segurança
  2) Instantaneamente, via "repository_dispatch", quando o Apps Script da
     planilha avisa o GitHub assim que alguém edita uma célula

A planilha precisa ter as mesmas colunas do Excel original, na mesma ordem:
CODIGO, TP, GRUPO, Descricao, U.M., FL, ARMZ, SALDO_EM_ESTOQUE,
EMPENHO_PARA_REQ_PV_RESERVA, ESTOQUE_DISPONIVEL, DT_ULT_MOV
(cabeçalho na linha 2, dados a partir da linha 3 - igual ao Excel).
"""

import os
import re
import csv
import json
import sys
from datetime import datetime, timedelta, timezone

import requests
from io import StringIO

CSV_URL = os.environ["GSHEET_CSV_URL"]
VALIDADES_CSV_URL = os.environ.get("GSHEET_VALIDADES_CSV_URL")
PRECOS_CSV_URL = os.environ.get("GSHEET_PRECOS_CSV_URL")
PEDIDOS_CSV_URL = os.environ.get("GSHEET_PEDIDOS_CSV_URL")
PROGRAMACAO_CSV_URL = os.environ.get("GSHEET_PROGRAMACAO_CSV_URL")

REPO_INDEX_PATH = "index.html"
REPO_PRECOS_PATH = "tabela-precos.html"
REPO_PEDIDOS_PATH = "pedidos-em-aberto.html"

# ── Nomes de exibição fixos por código ──
# Quando um card precisa ser (re)criado (item saiu e voltou pra planilha,
# ou é genuinamente novo), o nome mostrado vem DAQUI se o código estiver
# na lista - nunca da descrição bruta do ERP (que é longa/inconsistente).
# Só cai para a descrição do ERP quando o código não está mapeado aqui.
NOME_CANONICO = {
    "FRPTCANMI000045": "Filé de Peito Individual Cx Pp 20kg",
    "FRINCANMI000002": "Frango Inteiro",
    "FRASCANMI000037": "Asa",
    "FRASCANMI000027": "Coxinha",
    "FRCSCANMI000018": "Coxa e Sobrecoxa",
    "FRCSCANMI000016": "Sobrecoxa",
    "FRPTCANMI000065": "Filé de Peito",
    "FRPTCANMI000018": "Peito com osso",
    "FRMDCANMI000020": "Coração",
    "FRMDCANMI000008": "Moela",
    "FRCSCANMI000051": "Sobrecoxa",
    "FRPTCANMI000062": "Meio Filé",
    "FRPTCANMI000061": "Sassami",
    "FRASCANMI000047": "Coxinha",
    "FRASCANMI000048": "Meio da Asa",
    "FRCSCANMI000052": "Coxa Pilão",
    "FRMDBELME000007": "Fígado- Bandeja- BELLAVES",
    "FRMDBELME000008": "Moela- Bandeja- BELLAVES",
    "FRINCANMI000017": "Frango Carcaça Temperado",
    "FRINCANMI000003": "Frango inteiro",
    "FRINCANMI000004": "Frango temperado 18kg",
    "FRINCANMI000005": "Frango temperado 20kg",
    "FRINCANMI000006": "Frango temperado PV",
    "FRINMIFMI000009": "Carcaça 1,6 Mister",
    "FRINCANMI000011": "Carcaça 1,650/ 1,750",
    "FRINCANMI000012": "Carcaça 1,750/ 1,850",
    "FRINCANMI000013": "Carcaça 1,850/ 1,950",
    "FRINCANMI000014": "Carcaça 1,950/ 2,050",
    "FRINCANMI000015": "Carcaça 2,050/ 2,150",
    "FRINCANMI000016": "Carcaça 2,150/ 2,250",
    "COMPRO000010": "Galinha Inteira Congelada 22Kg",
    "COMPRO000011": "Galo Inteiro Congelado 22Kg",
    "COMPRO000083": "Galinha Inteira Congelada 20Kg",
    "COMPRO000084": "Galo Inteiro Congelado 20Kg",
    "FRASCANMI000039": "Asa- Interfolhada",
    "FRASCANMI000028": "Asa",
    "FRASCANMI000001": "Asa 18Kg",
    "FRCSCANMI000040": "Coxinha da Asa Cong. Bandeja 1kg",
    "FRASCANMI000041": "Coxinha da asa- Interfolhada",
    "FRASCANMI000029": "Coxinha de asa",
    "FRASCANMI000043": "Meio da asa c/osso) Pct 10 KG",
    "FRASCANMI000040": "Meio da asa - Interfolhado",
    "FRASCANMI000005": "Meio da Asa Interfolhado 18Kg",
    "FRASCANMI000007": "Meio da asa - Individual",
    "FRCSCANMI000006": "Coxa Pilão - Interfolhada",
    "FRCSCANMI000039": "Coxa Pilão Cong. Bandeja 1kg",
    "FRCSCANMI000041": "Sobrecoxa Cong. Bandeja 1kg",
    "FRCSCANMI000002": "Coxa e sobrecoxa- Interfolhada",
    "FRCSCANME000013": "Coxa e sobrecoxa- Interfolhado ME",
    "FRCSCANME000024": "Coxa e Sobrecoxa Interfolhado ME 15kg",
    "FRCSCANMI000001": "Coxa e sobrecoxa 18Kg",
    "FRCSCANMI000003": "Coxa e sobrecoxa",
    "FRCSCANMI000010": "Sobrecoxa IQF 6 Kg",
    "FRCSCANMI000019": "Filé de Coxa c/ Sobrecoxa Pct 2kg",
    "FRCSCANMI000053": "Filé de Coxa c/ Sobrecoxa Pct 1kg",
    "FRCSCANMI000027": "Coxa e sobrecoxa a passarinho Pct 10kg",
    "FRCSCANMI000023": "Coxa dessosada 800g",
    "FRCSCANMI000021": "Coxa dorsal",
    "FRCSCANMI000034": "Filé de coxa",
    "FRCSCANMI000005": "Sobrecoxa - Interfolhada",
    "FRCSCANME000001": "Filé de Sobrecoxa s/ Pele - Pct 2kg",
    "FRPTCANMI000033": "Filé de Peito - Individual",
    "FRPTCANMI000060": "Meio Filé - Pct 1Kg",
    "FRPTCANMI000053": "Filé de Peito \"Grade B\" - Pct 10kg",
    "FRPTCANMI000027": "Filé de Peito - Interfolhado",
    "FRPTCANMI000064": "Filé de Peito - Individual",
    "FRPTCANMI000008": "Meio Filé - Interfolhado",
    "FRPTCANMI000051": "Meio Filé - Interfolhado.",
    "FRPTCANME000004": "Meio Filé ME",
    "FRPTCANMI000059": "Meio Peito Cong. Bandeja 1kg",
    "FRPTCANMI000004": "Peito com osso",
    "FRPTCANMI000050": "Peito- Interfolhado",
    "FRPTCANMI000058": "Sassami Cong. Bandeja 1Kg",
    "FRPTCANMI000026": "Sassami - Interfolhado",
    "FRPTCANMI000043": "Sassami - Individual",
    "FRPTCANMI000046": "Sassami - Pct 1kg",
    "FRPTCANMI000041": "Sassami 6kg",
    "FRPTCANMI000048": "Sassami 800g",
    "FRPTCANMI000052": "Sassami \"Grade B\"- Pct 10kg",
    "FRMDCANMI000015": "Coração 1kg",
    "FRMDCANMI000029": "Coração 2kg",
    "FRMDCANMI000034": "Coração Cong. Bandeja 600g",
    "FRMDCANMI000016": "Fígado 1kg",
    "FRMDCANME000003": "Figado 1kg ME",
    "COMPRO000089": "Moela PCT 1 Kg Cx 18kg Mister Frango",
    "FRMDCANMI000022": "Moela 1kg",
    "FRPEMIFME000003": "Pés Grade Pct 7,5",
    "FRPECANME000003": "Pés Pct 7,5",
    "FRPECANMI000002": "Pés Carijo Pct 7,5",
    "COMPRO000088": "Pés Mister Frango 7,5kg",
    "FRMDCANMI000030": "Pescoço 1kg",
    "FRSACANMI000001": "Sambiquira 1kg",
    "FRSAMIFMI000001": "Sambiquira-Mister",
    "FRMRCANMI000002": "Recorte Filé Peito + Filé Coxa",
    "FRMSCANMI000005": "CMS",
    "FRMSCANME000006": "CMS Congelada - Bloco",
    "FRMSCANMI000008": "CMS- Bloco",
    "FRCSCANMI000009": "Coxa Pilão IQF 1kg",
    "FRCSCANMI000032": "Coxa e Sobrecoxa a Passarinho IQF 800g",
    "FRCSCANMI000059": "Coxa e Sobrecoxa a Passarinho IQF 1Kg",
    "FRASCANMI000022": "Coxinha IQF 800g",
    "FRASCANMI000010": "Coxinha IQF 1kg",
    "FRASCANMI000008": "Coxinha IQF 2kg",
    "FRASCANMI000023": "Meio da Asa IQF 800g",
    "FRASCANMI000011": "Meio da Asa IQF 1kg",
    "FRPTCANMI000054": "Filé de Peito em Bifes IQF 800g",
    "FRPTCANMI000069": "Filé de peito em Bifes IQF 1Kg",
    "FRPTCANMI000021": "Meio Filé IQF 1kg",
    "FRPTCANMI000024": "Sassami IQF 800g",
    "FRPTCANMI000010": "Sassami IQF 1kg",
    "FRPTCANMI000013": "Sassami IQF 6kg",
    "FRCSCANMI000055": "Filé de Sobrecoxa IQF 1Kg",
    "FRCSCANMI000011": "Sobrecoxa IQF 1kg",
    "PEFLCANMI000002": "Tilápia 400g",
    "PEFLCANMI000001": "Tilápia 800g",
    "PEFLCANMI000011": "Tilápia em pedaços",
    "PEFLCANMI000013": "Isca de Tilápia 400g",
    "PEFLCANMI000012": "Filé Tilápia Empanado 600g",
    "COMPRO000008": "Sassami Empanado 700g",
    "COMPRO000064": "Sassami Empanado 1kg",
    "COMPRO000065": "Chicken 300g",
    "COMPRO000066": "Chicken 1kg",
    "COMPRO000099": "Fut Chicken 300g",
    "COMPRO000100": "Fut Chicken 1kg",
    "COMPRO000121": "Coxinha da Asa empanada tradicional (Fut Wings)",
    "COMPRO000123": "Coxinha da Asa empanada apimentada (Fut Wings)",
    "COMPRO000074": "Anel de cebola 6,6kg",
    "COMPRO000009": "Anel de cebola 11kg",
    "COMPRO000068": "Batata 400g",
    "COMPRO000069": "Batata 1,1kg",
    "COMPRO000067": "Batata 2kg",
    "COMPRO000098": "Batata Noisette 400G",
    "COMPRO000003": "Mandioca",
    "COMPRO000081": "Pão de Queijo 300g",
    "COMPRO000082": "Pão de Queijo 800g",
    "COMPRO000093": "Pão de queijo de 300g",
    "COMPRO000059": "Polenta",
    "COMPRO000094": "Lasanha Bolonhesa 600g",
    "COMPRO000095": "Lasanha Quatro Queijos 600g",
    "COMPRO000096": "Lasanha Frango 600g",
    "COMPRO000097": "Bolinho de Tilápia 300g",
    "COMPRO000101": "Small Fish 400g",
    "COMPRO000102": "Small Fish 1,5kg",
    "COMPRO000116": "Fut Burguer c/ Molho de Picles",
    "COMPRO000117": "Fut Burguer Bacon c/ Requeijão",
    "COMPRO000118": "Fut Burguer Maionese Grill",
    "COMPRO000119": "Fut Chicken c/ Recheio de Queijo",
}


# ── Código Canção fixo por código ──
# Mesma lógica do NOME_CANONICO: quando um card precisa ser (re)criado,
# o Código Canção vem daqui (não fica '—') se o código estiver mapeado.
COD_CANCAO_FIXO = {
    # Gerado a partir do relatório de produtos (SB1) exportado do ERP -
    # coluna COD_CANCAO. Pra atualizar essa lista inteira de novo no
    # futuro (ex: muitos códigos novos de uma vez), me manda o mesmo
    # relatório atualizado que eu regenero. Pra 1 item isolado, o botão
    # "🏷️ Cód. Canção" no painel de item novo do site já resolve.
    "COMPRO000001": "53243",
    "COMPRO000002": "53244",
    "COMPRO000003": "53245",
    "COMPRO000004": "61676",
    "COMPRO000005": "62378",
    "COMPRO000006": "62382",
    "COMPRO000007": "62455",
    "COMPRO000008": "67772",
    "COMPRO000009": "67983",
    "COMPRO000010": "86590",
    "COMPRO000011": "86591",
    "COMPRO000012": "90220",
    "COMPRO000013": "47176",
    "COMPRO000014": "74931",
    "COMPRO000015": "82276",
    "COMPRO000016": "82277",
    "COMPRO000017": "68788",
    "COMPRO000018": "74433",
    "COMPRO000019": "86719",
    "COMPRO000020": "86720",
    "COMPRO000021": "90111",
    "COMPRO000022": "90138",
    "COMPRO000023": "90134",
    "COMPRO000024": "90132",
    "COMPRO000025": "90127",
    "COMPRO000026": "91021",
    "COMPRO000027": "91022",
    "COMPRO000028": "91023",
    "COMPRO000029": "91024",
    "COMPRO000030": "91000",
    "COMPRO000031": "91001",
    "COMPRO000032": "90141",
    "COMPRO000033": "91101",
    "COMPRO000034": "91147",
    "COMPRO000039": "91231",
    "COMPRO000040": "91232",
    "COMPRO000041": "91233",
    "COMPRO000042": "91234",
    "COMPRO000043": "91257",
    "COMPRO000044": "91236",
    "COMPRO000045": "91237",
    "COMPRO000046": "91238",
    "COMPRO000047": "91258",
    "COMPRO000048": "91240",
    "COMPRO000049": "91241",
    "COMPRO000050": "91242",
    "COMPRO000051": "91243",
    "COMPRO000052": "91244",
    "COMPRO000053": "91245",
    "COMPRO000054": "91264",
    "COMPRO000055": "91265",
    "COMPRO000056": "91269",
    "COMPRO000057": "91270",
    "COMPRO000058": "91271",
    "COMPRO000059": "91268",
    "COMPRO000060": "91281",
    "COMPRO000061": "91278",
    "COMPRO000062": "91275",
    "COMPRO000064": "91325",
    "COMPRO000065": "91326",
    "COMPRO000066": "91327",
    "COMPRO000067": "91328",
    "COMPRO000068": "91329",
    "COMPRO000069": "91330",
    "COMPRO000074": "91373",
    "COMPRO000081": "91375",
    "COMPRO000082": "91374",
    "COMPRO000083": "91357",
    "COMPRO000084": "91359",
    "COMPRO000085": "91358",
    "COMPRO000086": "91360",
    "COMPRO000087": "91361",
    "COMPRO000088": "91362",
    "COMPRO000089": "91363",
    "COMPRO000090": "91364",
    "COMPRO000094": "91415",
    "COMPRO000095": "91416",
    "COMPRO000096": "91414",
    "COMPRO000097": "91413",
    "COMPRO000098": "91408",
    "COMPRO000099": "91409",
    "COMPRO000100": "91410",
    "COMPRO000101": "91411",
    "COMPRO000102": "91412",
    "COMPRO000116": "91452",
    "COMPRO000117": "91453",
    "COMPRO000118": "91454",
    "COMPRO000119": "91455",
    "COMPRO000121": "91450",
    "COMPRO000123": "91451",
    "COMTER000001": "28858",
    "COMTER000002": "37304",
    "COMTER000003": "44616",
    "COMTER000004": "47071",
    "COMTER000005": "91005",
    "COMTER000006": "52984",
    "COMTER000007": "90490",
    "COMTER000008": "91006",
    "COMTER000009": "75589",
    "COMTER000010": "77546",
    "COMTER000011": "79270",
    "COMTER000012": "79581",
    "COMTER000013": "79604",
    "COMTER000014": "79874",
    "COMTER000015": "79991",
    "COMTER000016": "80095",
    "COMTER000017": "80762",
    "COMTER000018": "80848",
    "COMTER000019": "80883",
    "COMTER000020": "81323",
    "COMTER000021": "81324",
    "COMTER000022": "81873",
    "COMTER000023": "81917",
    "COMTER000024": "82127",
    "COMTER000025": "82128",
    "COMTER000026": "91007",
    "COMTER000027": "91096",
    "COMTER000028": "82354",
    "COMTER000029": "82438",
    "COMTER000030": "82497",
    "COMTER000031": "25320",
    "COMTER000032": "26060",
    "COMTER000033": "43624",
    "COMTER000034": "44606",
    "COMTER000035": "81748",
    "COMTER000036": "82213",
    "COMTER000038": "90128",
    "COMTER000039": "69751",
    "COMTER000040": "71413",
    "COMTER000041": "71934",
    "COMTER000042": "71935",
    "COMTER000043": "71937",
    "COMTER000044": "71938",
    "COMTER000045": "71936",
    "COMTER000046": "72478",
    "COMTER000047": "72474",
    "COMTER000048": "72473",
    "COMTER000049": "72479",
    "COMTER000050": "72477",
    "COMTER000053": "86643",
    "COMTER000054": "86644",
    "COMTER000055": "91097",
    "COMTER000056": "91008",
    "COMTER000057": "90231",
    "COMTER000058": "88625",
    "COMTER000059": "90489",
    "COMTER000060": "83203",
    "COMTER000061": "40355",
    "COMTER000062": "91025",
    "COMTER000063": "90516",
    "COMTER000064": "91098",
    "COMTER000065": "91099",
    "COMTER000066": "89954",
    "COMTER000067": "91100",
    "COMTER000069": "91095",
    "COMTER000070": "85863",
    "COMTER000073": "90392",
    "COMTER000075": "89771",
    "COMTER000077": "64596",
    "COMTER000078": "85877",
    "COMTER000079": "88517",
    "COMTER000080": "90488",
    "COMTER000082": "86318",
    "COMTER000083": "84037",
    "COMTER000084": "85506",
    "COMTER000087": "89762",
    "COMTER000090": "88626",
    "COMTER000091": "84033",
    "COMTER000093": "91094",
    "COMTER000094": "84034",
    "COMTER000095": "85505",
    "COMTER000096": "84253",
    "COMTER000097": "91093",
    "COMTER000098": "84118",
    "COMTER000099": "91092",
    "COMTER000100": "88579",
    "COMTER000101": "69771",
    "COMTER000102": "85261",
    "COMTER000103": "85271",
    "COMTER000104": "85734",
    "COMTER000105": "91090",
    "COMTER000106": "91091",
    "COMTER000107": "87893",
    "COMTER000108": "88212",
    "COMTER000109": "88214",
    "COMTER000110": "88135",
    "COMTER000111": "88031",
    "COMTER000112": "88032",
    "COMTER000113": "88033",
    "COMTER000114": "86587",
    "COMTER000115": "88284",
    "COMTER000116": "88285",
    "COMTER000117": "88286",
    "COMTER000118": "88288",
    "COMTER000119": "89019",
    "COMTER000120": "89234",
    "COMTER000121": "89236",
    "COMTER000122": "89237",
    "COMTER000123": "88699",
    "COMTER000124": "88624",
    "COMTER000125": "91089",
    "COMTER000126": "91026",
    "COMTER000127": "91002",
    "COMTER000128": "90648",
    "COMTER000129": "91004",
    "COMTER000131": "91003",
    "COMTER000133": "91027",
    "COMTER000134": "91031",
    "COMTER000135": "91088",
    "COMTER000136": "91102",
    "COMTER000138": "91087",
    "COMTER000139": "91083",
    "COMTER000140": "91084",
    "COMTER000141": "91085",
    "COMTER000142": "91086",
    "COMTER000143": "91080",
    "COMTER000144": "91081",
    "COMTER000145": "91082",
    "COMTER000146": "91113",
    "COMTER000147": "91114",
    "COMTER000148": "91115",
    "COMTER000149": "91116",
    "COMTER000150": "91117",
    "COMTER000151": "91118",
    "COMTER000152": "91119",
    "COMTER000153": "91112",
    "COMTER000154": "91120",
    "COMTER000155": "91121",
    "COMTER000156": "91133",
    "COMTER000157": "91132",
    "COMTER000158": "91131",
    "COMTER000159": "91124",
    "COMTER000160": "91130",
    "COMTER000161": "91126",
    "COMTER000162": "91125",
    "COMTER000163": "91122",
    "COMTER000164": "91123",
    "COMTER000165": "91138",
    "COMTER000166": "91135",
    "COMTER000167": "91139",
    "COMTER000168": "91140",
    "COMTER000169": "91141",
    "COMTER000170": "91142",
    "COMTER000171": "91143",
    "COMTER000172": "91144",
    "COMTER000173": "91145",
    "COMTER000174": "91146",
    "COMTER000175": "91148",
    "COMTER000176": "91149",
    "COMTER000177": "91150",
    "COMTER000178": "91151",
    "COMTER000179": "91152",
    "COMTER000180": "91153",
    "COMTER000181": "91154",
    "COMTER000182": "91155",
    "COMTER000183": "91156",
    "COMTER000184": "91157",
    "COMTER000185": "91158",
    "COMTER000186": "91159",
    "COMTER000187": "91160",
    "COMTER000188": "91161",
    "COMTER000189": "91162",
    "COMTER000190": "91163",
    "COMTER000191": "91164",
    "COMTER000192": "91165",
    "COMTER000193": "91166",
    "COMTER000194": "91167",
    "COMTER000195": "91168",
    "COMTER000196": "91169",
    "COMTER000205": "91180",
    "COMTER000206": "91384",
    "COMTER000207": "91212",
    "COMTER000208": "91228",
    "COMTER000209": "91230",
    "COMTER000210": "91229",
    "COMTER000211": "91173",
    "COMTER000212": "91174",
    "COMTER000213": "91175",
    "COMTER000214": "91176",
    "COMTER000215": "91177",
    "COMTER000216": "91178",
    "COMTER000217": "91179",
    "COMTER000218": "91191",
    "COMTER000219": "91190",
    "COMTER000220": "91189",
    "COMTER000221": "91188",
    "COMTER000222": "91187",
    "COMTER000223": "91214",
    "COMTER000224": "91181",
    "COMTER000225": "91182",
    "COMTER000226": "91183",
    "COMTER000227": "91184",
    "COMTER000228": "91185",
    "COMTER000230": "91208",
    "COMTER000231": "91195",
    "COMTER000232": "91196",
    "COMTER000233": "91207",
    "COMTER000234": "91197",
    "COMTER000235": "91198",
    "COMTER000236": "91199",
    "COMTER000237": "91200",
    "COMTER000238": "91201",
    "COMTER000239": "91193",
    "COMTER000240": "91202",
    "COMTER000241": "91203",
    "COMTER000242": "91204",
    "COMTER000243": "91205",
    "COMTER000244": "91206",
    "COMTER000245": "91216",
    "COMTER000246": "91217",
    "COMTER000247": "91227",
    "COMTER000248": "91226",
    "COMTER000249": "91225",
    "COMTER000250": "91224",
    "COMTER000251": "91223",
    "COMTER000252": "91222",
    "COMTER000253": "91221",
    "COMTER000254": "91220",
    "COMTER000255": "91219",
    "COMTER000256": "91218",
    "COMTER000257": "91210",
    "COMTER000258": "91211",
    "COMTER000259": "91251",
    "COMTER000260": "91252",
    "COMTER000261": "91246",
    "COMTER000262": "91285",
    "COMTER000265": "91261",
    "COMTER000266": "91266",
    "COMTER000267": "91267",
    "COMTER000268": "91292",
    "COMTER000270": "91276",
    "COMTER000271": "91280",
    "COMTER000272": "91286",
    "COMTER000273": "91287",
    "COMTER000274": "91273",
    "COMTER000276": "91284",
    "COMTER000277": "91282",
    "COMTER000279": "91446",
    "COMTER000280": "91290",
    "COMTER000281": "91291",
    "COMTER000282": "91293",
    "COMTER000284": "91295",
    "COMTER000285": "91297",
    "COMTER000286": "91301",
    "COMTER000287": "91302",
    "COMTER000288": "91303",
    "COMTER000289": "91304",
    "COMTER000290": "91306",
    "COMTER000291": "91307",
    "COMTER000292": "91313",
    "COMTER000293": "91314",
    "COMTER000294": "91316",
    "COMTER000295": "91318",
    "COMTER000296": "91323",
    "COMTER000297": "91331",
    "COMTER000298": "91332",
    "COMTER000299": "91333",
    "COMTER000302": "91442",
    "COMTER000303": "91345",
    "COMTER000304": "91344",
    "COMTER000305": "91354",
    "COMTER000306": "91355",
    "COMTER000307": "91356",
    "COMTER000310": "91403",
    "COMTER000311": "91405",
    "COMTER000312": "91406",
    "COMTER000313": "91407",
    "COMTER000315": "91420",
    "COMTER000316": "91421",
    "COMTER000317": "91422",
    "COMTER000318": "91423",
    "COMTER001608": "91435",
    "COMTER001666": "91445",
    "FRARCANME000001": "69898",
    "FRARCANME000002": "91338",
    "FRARMIFME000001": "83811",
    "FRARMIFME000002": "33956",
    "FRARMIFME000003": "33955",
    "FRARMIFME000004": "83725",
    "FRASAGOMI000001": "48551",
    "FRASAGOMI000002": "48562",
    "FRASAGOMI000003": "48563",
    "FRASAGOMI000004": "48564",
    "FRASAGOMI000005": "54035",
    "FRASAGOMI000006": "71908",
    "FRASAGOMI000007": "78597",
    "FRASBELME000001": "82227",
    "FRASBELME000002": "82228",
    "FRASBELME000003": "91260",
    "FRASBELME000006": "84117",
    "FRASBELME000007": "91311",
    "FRASBELME000008": "86582",
    "FRASCANME000001": "10142",
    "FRASCANME000002": "10143",
    "FRASCANME000003": "33122",
    "FRASCANME000004": "68022",
    "FRASCANME000005": "68051",
    "FRASCANME000006": "68408",
    "FRASCANME000007": "17313",
    "FRASCANMI000001": "2352",
    "FRASCANMI000002": "2353",
    "FRASCANMI000003": "2355",
    "FRASCANMI000004": "9830",
    "FRASCANMI000005": "9831",
    "FRASCANMI000006": "10364",
    "FRASCANMI000007": "10420",
    "FRASCANMI000008": "89772",
    "FRASCANMI000009": "28626",
    "FRASCANMI000010": "62157",
    "FRASCANMI000011": "62158",
    "FRASCANMI000012": "64343",
    "FRASCANMI000013": "64678",
    "FRASCANMI000014": "71310",
    "FRASCANMI000016": "12914",
    "FRASCANMI000017": "71301",
    "FRASCANMI000018": "90135",
    "FRASCANMI000021": "91028",
    "FRASCANMI000022": "91170",
    "FRASCANMI000023": "91319",
    "FRASCANMI000024": "91171",
    "FRASCANMI000025": "91103",
    "FRASCANMI000026": "91079",
    "FRASCANMI000027": "91192",
    "FRASCANMI000028": "91172",
    "FRASCANMI000029": "91308",
    "FRASCANMI000033": "91209",
    "FRASCANMI000034": "91250",
    "FRASCANMI000035": "91277",
    "FRASCANMI000037": "91321",
    "FRASCANMI000039": "91320",
    "FRASCANMI000040": "91315",
    "FRASCANMI000041": "91322",
    "FRASCANMI000043": "91391",
    "FRASCANMI000044": "91383",
    "FRASCANMI000047": "91396",
    "FRASCANMI000048": "91402",
    "FRASCANMI000050": "91449",
    "FRASCANMI000051": "91448",
    "FRASGOLME000001": "85658",
    "FRASGOLME000002": "80683",
    "FRASGOLMI000001": "21541",
    "FRASGOLMI000002": "84780",
    "FRASGOLMI000003": "91104",
    "FRASGOLMI000004": "91255",
    "FRASMIFME000001": "33948",
    "FRASMIFME000002": "33950",
    "FRASMIFME000003": "52058",
    "FRASMIFME000004": "69894",
    "FRASMIFME000005": "73939",
    "FRASMIFME000006": "73938",
    "FRASMIFMI000001": "33899",
    "FRASMIFMI000002": "33900",
    "FRASMIFMI000003": "33901",
    "FRASMIFMI000004": "33902",
    "FRASMIFMI000005": "35037",
    "FRASMIFMI000006": "82780",
    "FRASMIFMI000008": "75112",
    "FRASMIFMI000009": "33926",
    "FRASMIFMI000010": "69253",
    "FRCRCANMI000001": "62863",
    "FRCSAGOMI000001": "49216",
    "FRCSAGOMI000002": "49215",
    "FRCSAGOMI000003": "78212",
    "FRCSAGOMI000004": "78605",
    "FRCSAGOMI000005": "78599",
    "FRCSAGOMI000006": "85876",
    "FRCSAGOMI000007": "48585",
    "FRCSAGOMI000008": "49340",
    "FRCSBELME000001": "47178",
    "FRCSBELME000002": "85869",
    "FRCSBELME000003": "83375",
    "FRCSCANME000001": "12719",
    "FRCSCANME000002": "13216",
    "FRCSCANME000003": "16876",
    "FRCSCANME000004": "16877",
    "FRCSCANME000005": "16878",
    "FRCSCANME000006": "16879",
    "FRCSCANME000007": "17514",
    "FRCSCANME000008": "17515",
    "FRCSCANME000009": "26668",
    "FRCSCANME000010": "13642",
    "FRCSCANME000013": "68405",
    "FRCSCANME000014": "14352",
    "FRCSCANME000015": "16860",
    "FRCSCANME000016": "17311",
    "FRCSCANME000017": "68406",
    "FRCSCANME000018": "69208",
    "FRCSCANME000019": "85477",
    "FRCSCANME000020": "79580",
    "FRCSCANME000021": "77787",
    "FRCSCANME000022": "84119",
    "FRCSCANME000023": "78498",
    "FRCSCANME000024": "91336",
    "FRCSCANME000025": "91342",
    "FRCSCANME000026": "91337",
    "FRCSCANME000028": "91418",
    "FRCSCANME000029": "91437",
    "FRCSCANME000030": "2687",
    "FRCSCANME000032": "91447",
    "FRCSCANMI000001": "2359",
    "FRCSCANMI000002": "2363",
    "FRCSCANMI000003": "2364",
    "FRCSCANMI000004": "2367",
    "FRCSCANMI000005": "2398",
    "FRCSCANMI000006": "8143",
    "FRCSCANMI000007": "9066",
    "FRCSCANMI000008": "9806",
    "FRCSCANMI000009": "62160",
    "FRCSCANMI000010": "64345",
    "FRCSCANMI000011": "64347",
    "FRCSCANMI000012": "64676",
    "FRCSCANMI000013": "64677",
    "FRCSCANMI000014": "64342",
    "FRCSCANMI000015": "71306",
    "FRCSCANMI000016": "71315",
    "FRCSCANMI000017": "71309",
    "FRCSCANMI000018": "84374",
    "FRCSCANMI000019": "79563",
    "FRCSCANMI000020": "90545",
    "FRCSCANMI000021": "27441",
    "FRCSCANMI000022": "91029",
    "FRCSCANMI000023": "91030",
    "FRCSCANMI000024": "91111",
    "FRCSCANMI000026": "91215",
    "FRCSCANMI000027": "91404",
    "FRCSCANMI000032": "91335",
    "FRCSCANMI000033": "91334",
    "FRCSCANMI000034": "91343",
    "FRCSCANMI000037": "91365",
    "FRCSCANMI000038": "91366",
    "FRCSCANMI000039": "91400",
    "FRCSCANMI000040": "91432",
    "FRCSCANMI000041": "91440",
    "FRCSCANMI000051": "91395",
    "FRCSCANMI000052": "91401",
    "FRCSCANMI000053": "91429",
    "FRCSCANMI000055": "91438",
    "FRCSCANMI000059": "91436",
    "FRCSCANMI000062": "91431",
    "FRCSGOLME000001": "84779",
    "FRCSMIFME000001": "33961",
    "FRCSMIFME000002": "33962",
    "FRCSMIFME000003": "33963",
    "FRCSMIFME000004": "33964",
    "FRCSMIFME000005": "33965",
    "FRCSMIFME000006": "33967",
    "FRCSMIFME000007": "33971",
    "FRCSMIFME000008": "44989",
    "FRCSMIFME000009": "51058",
    "FRCSMIFME000010": "81605",
    "FRCSMIFMI000001": "33903",
    "FRCSMIFMI000002": "33906",
    "FRCSMIFMI000003": "33907",
    "FRCSMIFMI000004": "33925",
    "FRCSMIFMI000005": "35047",
    "FRCSMIFMI000007": "47956",
    "FRCSMIFMI000008": "85815",
    "FRCSMIFMI000009": "85516",
    "FRCSMIFMI000010": "85518",
    "FRCSMIFMI000011": "86586",
    "FRCSMIFMI000012": "86583",
    "FRCSMIFMI000013": "91274",
    "FRCSMIFMI000015": "91253",
    "FRINAGOMI000001": "80390",
    "FRINAGOMI000002": "80388",
    "FRINAGOMI000003": "80389",
    "FRINAGOMI000005": "78603",
    "FRINAGOMI000006": "79903",
    "FRINAGOMI000007": "79929",
    "FRINAGOMI000008": "47209",
    "FRINAGOMI000014": "79900",
    "FRINAGOMI000015": "79902",
    "FRINAGOMI000016": "79901",
    "FRINAGOMI000018": "54013",
    "FRINAGOMI000019": "81881",
    "FRINAGOMI000021": "70118",
    "FRINAGOMI000022": "87906",
    "FRINAGOMI000023": "49150",
    "FRINAGOMI000024": "49151",
    "FRINAGOMI000025": "49152",
    "FRINAGOMI000026": "49153",
    "FRINAGOMI000027": "49154",
    "FRINAGOMI000028": "53121",
    "FRINAGOMI000029": "54012",
    "FRINAGOMI000030": "80391",
    "FRINAGOMI000031": "81880",
    "FRINAGOMI000032": "70088",
    "FRINBELME000001": "43828",
    "FRINBELME000002": "65898",
    "FRINBELME000003": "65897",
    "FRINBELME000004": "65891",
    "FRINBELME000005": "65889",
    "FRINBELME000006": "65892",
    "FRINBELME000007": "65890",
    "FRINBELME000009": "65893",
    "FRINBELME000010": "65894",
    "FRINCANME000001": "15666",
    "FRINCANME000002": "52747",
    "FRINCANMI000001": "27988",
    "FRINCANMI000002": "69964",
    "FRINCANMI000003": "69963",
    "FRINCANMI000004": "91077",
    "FRINCANMI000005": "91078",
    "FRINCANMI000006": "91110",
    "FRINCANMI000007": "91136",
    "FRINCANMI000008": "91137",
    "FRINCANMI000011": "91346",
    "FRINCANMI000012": "91347",
    "FRINCANMI000013": "91348",
    "FRINCANMI000014": "91349",
    "FRINCANMI000015": "91350",
    "FRINCANMI000016": "91351",
    "FRINCANMI000017": "91417",
    "FRINMIFMI000001": "69456",
    "FRINMIFMI000002": "73982",
    "FRINMIFMI000007": "86584",
    "FRINMIFMI000008": "86585",
    "FRINMIFMI000009": "68158",
    "FRINMIFMI000010": "68159",
    "FRINMIFMI000011": "68169",
    "FRINMIFMI000012": "68170",
    "FRINMIFMI000013": "67870",
    "FRINMIFMI000014": "67875",
    "FRINMIFMI000015": "91106",
    "FRINMIFMI000016": "91107",
    "FRINMIFMI000017": "91075",
    "FRMDAGOMI000001": "48565",
    "FRMDAGOMI000002": "48569",
    "FRMDAGOMI000003": "65256",
    "FRMDAGOMI000004": "78710",
    "FRMDAGOMI000005": "78606",
    "FRMDAGOMI000006": "78598",
    "FRMDBELME000001": "47153",
    "FRMDBELME000002": "47155",
    "FRMDBELME000003": "60407",
    "FRMDBELME000004": "63590",
    "FRMDBELME000006": "68968",
    "FRMDBELME000007": "85851",
    "FRMDBELME000008": "85852",
    "FRMDCANME000001": "29987",
    "FRMDCANME000002": "29991",
    "FRMDCANME000003": "70209",
    "FRMDCANME000004": "69070",
    "FRMDCANME000005": "91283",
    "FRMDCANMI000001": "2356",
    "FRMDCANMI000002": "2357",
    "FRMDCANMI000003": "2369",
    "FRMDCANMI000004": "2370",
    "FRMDCANMI000005": "2383",
    "FRMDCANMI000006": "2384",
    "FRMDCANMI000007": "71311",
    "FRMDCANMI000008": "71313",
    "FRMDCANMI000009": "83322",
    "FRMDCANMI000010": "71302",
    "FRMDCANMI000011": "75393",
    "FRMDCANMI000012": "76092",
    "FRMDCANMI000013": "71422",
    "FRMDCANMI000014": "90555",
    "FRMDCANMI000015": "91263",
    "FRMDCANMI000016": "91254",
    "FRMDCANMI000017": "91305",
    "FRMDCANMI000018": "91213",
    "FRMDCANMI000019": "91376",
    "FRMDCANMI000020": "91247",
    "FRMDCANMI000021": "91248",
    "FRMDCANMI000022": "91289",
    "FRMDCANMI000029": "91296",
    "FRMDCANMI000030": "91299",
    "FRMDCANMI000031": "91433",
    "FRMDCANMI000032": "91371",
    "FRMDCANMI000034": "91428",
    "FRMDGOLMI000001": "70774",
    "FRMDMIFME000001": "35053",
    "FRMDMIFME000002": "61012",
    "FRMDMIFME000003": "71685",
    "FRMDMIFME000004": "85474",
    "FRMDMIFME000005": "72671",
    "FRMDMIFMI000001": "61348",
    "FRMDMIFMI000002": "68172",
    "FRMDMIFMI000003": "40200",
    "FRMDMIFMI000004": "71260",
    "FRMDMIFMI000005": "85106",
    "FRMDMIFMI000006": "84783",
    "FRMDMIFMI000007": "85105",
    "FRMDMIFMI000008": "86592",
    "FRMDMIFMI000009": "87468",
    "FRMDMIFMI000010": "37421",
    "FRMDMIFMI000011": "86588",
    "FRMDMIFMI000013": "91031",
    "FRMDMIFMI000014": "91108",
    "FRMDMIFMI000016": "91249",
    "FRMRCANMI000002": "54176",
    "FRMRMIFMI000002": "33939",
    "FRMSAGOMI000001": "47219",
    "FRMSBELME000001": "65940",
    "FRMSCANME000001": "70611",
    "FRMSCANME000002": "50529",
    "FRMSCANME000006": "91389",
    "FRMSCANMI000001": "9640",
    "FRMSCANMI000002": "82511",
    "FRMSCANMI000003": "90552",
    "FRMSCANMI000004": "64073",
    "FRMSCANMI000005": "64076",
    "FRMSCANMI000006": "91377",
    "FRMSCANMI000008": "69069",
    "FRMSCANMI000009": "70119",
    "FRMSCANMI000012": "91443",
    "FRMSMIFMI000001": "33919",
    "FRMSMIFMI000003": "34846",
    "FRMSMIFMI000004": "90554",
    "FRPACANME000001": "91010",
    "FRPACANME000002": "91011",
    "FRPAMIFME000001": "63386",
    "FRPAMIFME000002": "63387",
    "FRPAMIFME000003": "63388",
    "FRPAMIFME000004": "63389",
    "FRPEAGOME000001": "65132",
    "FRPEAGOMI000001": "78651",
    "FRPEAGOMI000002": "60519",
    "FRPECANME000001": "68083",
    "FRPECANME000002": "68084",
    "FRPECANME000003": "91294",
    "FRPECANME000004": "91298",
    "FRPECANMI000001": "91012",
    "FRPECANMI000002": "91392",
    "FRPEMIFME000001": "33957",
    "FRPEMIFME000002": "33958",
    "FRPEMIFME000003": "91272",
    "FRPEMIFME000004": "91368",
    "FRPEMIFMI000001": "89146",
    "FRPEMIFMI000002": "91076",
    "FRPLCANMI000001": "68372",
    "FRPLCANMI000002": "90562",
    "FRPTAGOMI000001": "48550",
    "FRPTAGOMI000003": "49214",
    "FRPTAGOMI000004": "78601",
    "FRPTAGOMI000005": "76685",
    "FRPTAGOMI000006": "72075",
    "FRPTAGOMI000007": "48586",
    "FRPTAGOMI000008": "48568",
    "FRPTAGOMI000009": "49486",
    "FRPTBELME000001": "50003",
    "FRPTBELME000002": "82229",
    "FRPTBELME000003": "82230",
    "FRPTBELME000004": "82231",
    "FRPTBELME000005": "63506",
    "FRPTBELME000006": "63504",
    "FRPTBELME000007": "53982",
    "FRPTBELME000008": "53980",
    "FRPTBELME000009": "71535",
    "FRPTBELME000010": "74915",
    "FRPTBELME000011": "91013",
    "FRPTBELME000012": "91367",
    "FRPTCANME000001": "91259",
    "FRPTCANME000002": "91256",
    "FRPTCANME000003": "64671",
    "FRPTCANME000004": "15776",
    "FRPTCANME000005": "16865",
    "FRPTCANME000006": "13593",
    "FRPTCANME000007": "85277",
    "FRPTCANME000008": "91394",
    "FRPTCANME000010": "91419",
    "FRPTCANME000014": "91426",
    "FRPTCANME000015": "91441",
    "FRPTCANMI000001": "2372",
    "FRPTCANMI000002": "2373",
    "FRPTCANMI000003": "2386",
    "FRPTCANMI000004": "2392",
    "FRPTCANMI000005": "2394",
    "FRPTCANMI000006": "8285",
    "FRPTCANMI000007": "15969",
    "FRPTCANMI000008": "31489",
    "FRPTCANMI000009": "48235",
    "FRPTCANMI000010": "62159",
    "FRPTCANMI000011": "62161",
    "FRPTCANMI000012": "64344",
    "FRPTCANMI000013": "64346",
    "FRPTCANMI000014": "64680",
    "FRPTCANMI000015": "71314",
    "FRPTCANMI000016": "71312",
    "FRPTCANMI000017": "84375",
    "FRPTCANMI000018": "84376",
    "FRPTCANMI000019": "69171",
    "FRPTCANMI000020": "72349",
    "FRPTCANMI000021": "88846",
    "FRPTCANMI000022": "88856",
    "FRPTCANMI000023": "90136",
    "FRPTCANMI000024": "91134",
    "FRPTCANMI000025": "91378",
    "FRPTCANMI000026": "91341",
    "FRPTCANMI000027": "91339",
    "FRPTCANMI000033": "91194",
    "FRPTCANMI000034": "91262",
    "FRPTCANMI000035": "91379",
    "FRPTCANMI000040": "91380",
    "FRPTCANMI000041": "91300",
    "FRPTCANMI000043": "91309",
    "FRPTCANMI000044": "91310",
    "FRPTCANMI000045": "91324",
    "FRPTCANMI000046": "91317",
    "FRPTCANMI000047": "91381",
    "FRPTCANMI000048": "91312",
    "FRPTCANMI000050": "91340",
    "FRPTCANMI000051": "91372",
    "FRPTCANMI000052": "91385",
    "FRPTCANMI000053": "91386",
    "FRPTCANMI000054": "91382",
    "FRPTCANMI000055": "91370",
    "FRPTCANMI000056": "91393",
    "FRPTCANMI000057": "91390",
    "FRPTCANMI000058": "91439",
    "FRPTCANMI000059": "91434",
    "FRPTCANMI000060": "91427",
    "FRPTCANMI000061": "91399",
    "FRPTCANMI000062": "91397",
    "FRPTCANMI000063": "91398",
    "FRPTCANMI000064": "91425",
    "FRPTCANMI000065": "91424",
    "FRPTCANMI000069": "91444",
    "FRPTCANMI000070": "91430",
    "FRPTGOLME000001": "80634",
    "FRPTGOLMI000001": "21373",
    "FRPTMIFMI000001": "33920",
    "FRPTMIFMI000002": "33922",
    "FRPTMIFMI000003": "33923",
    "FRPTMIFMI000004": "83196",
    "FRPTMIFMI000005": "89856",
    "FRPTMIFMI000006": "33937",
    "FRPTMIFMI000007": "75962",
    "FRPTMIFMI000008": "71477",
    "FRPTMIFMI000009": "70936",
    "FRPTMIFMI000010": "74568",
    "FRPTMIFMI000011": "75287",
    "FRPTMIFMI000012": "86589",
    "FRPTMIFMI000013": "33921",
    "FRPTMIFMI000014": "88790",
    "FRPTMIFMI000016": "89857",
    "FRPTMIFMI000017": "36632",
    "FRSACANME000001": "91109",
    "FRSACANMI000001": "70142",
    "FRSAMIFMI000001": "86779",
    "LOMEX000001": "67749",
    "LOMEX000002": "67789",
    "LOMEX000003": "69589",
    "LOMEX000004": "69590",
    "LOMEX000005": "69591",
    "LOMEX000006": "69592",
    "LOMEX000007": "69593",
    "LOMEX000008": "69594",
    "LOMEX000009": "69595",
    "LOMEX000010": "69596",
    "LOMEX000011": "69597",
    "LOMEX000012": "69598",
    "LOMEX000013": "69599",
    "LOMEX000014": "69600",
    "LOMEX000015": "69601",
    "LOMEX000016": "69602",
    "LOMEX000017": "70229",
    "LOMEX000018": "70401",
    "LOMEX000019": "71504",
    "LOMEX000020": "72074",
    "LOMEX000021": "72409",
    "LOMEX000022": "73705",
    "LOMEX000023": "74913",
    "LOMEX000024": "74918",
    "LOMEX000025": "76795",
    "LOMEX000026": "77576",
    "LOMEX000027": "78046",
    "LOMEX000028": "78349",
    "LOMEX000029": "65660",
    "LOMEX000030": "82121",
    "LOMEX000031": "81956",
    "LOMEX000032": "82452",
    "LOMEX000033": "82595",
    "LOMEX000034": "82936",
    "LOMEX000035": "65529",
    "LOMEX000036": "65535",
    "LOMEX000037": "76563",
    "LOMEX000038": "78338",
    "LOMEX000039": "68696",
    "LOMEX000040": "83260",
    "LOMEX000041": "84148",
    "LOMEX000042": "84301",
    "LOMEX000043": "84776",
    "LOMEX000044": "85873",
    "LOMEX000045": "85874",
    "LOMEX000046": "86773",
    "LOMEX000047": "87242",
    "LOMEX000048": "87243",
    "LOMEX000049": "87244",
    "LOMEX000050": "87245",
    "LOMEX000051": "87246",
    "LOMEX000052": "87280",
    "LOMEX000053": "87432",
    "LOMEX000054": "88058",
    "LOMEX000055": "88059",
    "LOMEX000056": "88060",
    "LOMEX000057": "88061",
    "LOMEX000058": "88054",
    "LOMEX000059": "88056",
    "LOMEX000060": "88390",
    "LOMEX000061": "88391",
    "LOMEX000062": "88841",
    "LOMEX000063": "88979",
    "LOMEX000064": "88980",
    "LOMEX000065": "89036",
    "LOMEX000066": "89037",
    "LOMEX000067": "89232",
    "LOMEX000068": "89798",
    "LOMEX000069": "89799",
    "LOMEX000070": "90396",
    "LOMEX000071": "82120",
    "LOMEX000072": "82122",
    "LOMEX000074": "91032",
    "LOMEX000077": "91015",
    "LOMEX000079": "91033",
    "LOMEX000080": "91016",
    "LOMEX000082": "91034",
    "LOMEX000083": "91035",
    "LOMEX000084": "91036",
    "LOMIN000001": "65272",
    "LOMIN000002": "65505",
    "LOMIN000003": "65506",
    "LOMIN000004": "65509",
    "LOMIN000005": "65514",
    "LOMIN000006": "65515",
    "LOMIN000007": "65516",
    "LOMIN000008": "65518",
    "LOMIN000009": "65519",
    "LOMIN000010": "65520",
    "LOMIN000011": "65521",
    "LOMIN000012": "65522",
    "LOMIN000013": "65523",
    "LOMIN000014": "65524",
    "LOMIN000015": "65525",
    "LOMIN000016": "65526",
    "LOMIN000017": "65527",
    "LOMIN000018": "65562",
    "LOMIN000019": "65563",
    "LOMIN000020": "65564",
    "LOMIN000021": "65565",
    "LOMIN000022": "65567",
    "LOMIN000023": "65568",
    "LOMIN000024": "65569",
    "LOMIN000025": "65570",
    "LOMIN000026": "65571",
    "LOMIN000027": "65572",
    "LOMIN000028": "65573",
    "LOMIN000029": "65574",
    "LOMIN000030": "65575",
    "LOMIN000031": "65576",
    "LOMIN000032": "65577",
    "LOMIN000033": "65578",
    "LOMIN000034": "65579",
    "LOMIN000035": "65580",
    "LOMIN000036": "65581",
    "LOMIN000037": "65582",
    "LOMIN000038": "65583",
    "LOMIN000039": "65584",
    "LOMIN000040": "65585",
    "LOMIN000041": "65586",
    "LOMIN000042": "65587",
    "LOMIN000043": "65588",
    "LOMIN000044": "65590",
    "LOMIN000045": "65591",
    "LOMIN000046": "65592",
    "LOMIN000047": "65595",
    "LOMIN000048": "65596",
    "LOMIN000049": "65597",
    "LOMIN000050": "65598",
    "LOMIN000051": "65599",
    "LOMIN000052": "65600",
    "LOMIN000053": "65601",
    "LOMIN000054": "65602",
    "LOMIN000055": "65603",
    "LOMIN000056": "65604",
    "LOMIN000057": "65605",
    "LOMIN000058": "65606",
    "LOMIN000059": "65607",
    "LOMIN000060": "65608",
    "LOMIN000061": "65609",
    "LOMIN000062": "65610",
    "LOMIN000063": "65611",
    "LOMIN000064": "65612",
    "LOMIN000065": "65614",
    "LOMIN000066": "65615",
    "LOMIN000067": "65616",
    "LOMIN000068": "65617",
    "LOMIN000069": "65618",
    "LOMIN000070": "65619",
    "LOMIN000071": "65620",
    "LOMIN000072": "65622",
    "LOMIN000073": "65623",
    "LOMIN000074": "65624",
    "LOMIN000075": "65626",
    "LOMIN000076": "65627",
    "LOMIN000077": "65628",
    "LOMIN000078": "65629",
    "LOMIN000079": "65630",
    "LOMIN000080": "65631",
    "LOMIN000081": "65632",
    "LOMIN000082": "65633",
    "LOMIN000083": "65635",
    "LOMIN000084": "65636",
    "LOMIN000085": "65637",
    "LOMIN000086": "65638",
    "LOMIN000087": "65639",
    "LOMIN000088": "65640",
    "LOMIN000089": "65641",
    "LOMIN000090": "65642",
    "LOMIN000091": "65643",
    "LOMIN000092": "65644",
    "LOMIN000093": "65645",
    "LOMIN000094": "65647",
    "LOMIN000095": "65674",
    "LOMIN000096": "65695",
    "LOMIN000097": "65752",
    "LOMIN000098": "67011",
    "LOMIN000099": "67048",
    "LOMIN000100": "67049",
    "LOMIN000101": "67097",
    "LOMIN000102": "67140",
    "LOMIN000103": "67496",
    "LOMIN000104": "67658",
    "LOMIN000105": "67659",
    "LOMIN000106": "67808",
    "LOMIN000107": "67916",
    "LOMIN000108": "68025",
    "LOMIN000109": "68026",
    "LOMIN000110": "68299",
    "LOMIN000111": "68300",
    "LOMIN000112": "68301",
    "LOMIN000113": "68302",
    "LOMIN000114": "68303",
    "LOMIN000115": "68304",
    "LOMIN000116": "68316",
    "LOMIN000117": "68531",
    "LOMIN000118": "69493",
    "LOMIN000119": "84329",
    "LOMIN000120": "83809",
    "LOMIN000121": "68892",
    "LOMIN000122": "69765",
    "LOMIN000123": "69831",
    "LOMIN000124": "71988",
    "LOMIN000125": "71665",
    "LOMIN000126": "71666",
    "LOMIN000127": "71672",
    "LOMIN000128": "72044",
    "LOMIN000129": "84149",
    "LOMIN000130": "72175",
    "LOMIN000131": "72709",
    "LOMIN000132": "74995",
    "LOMIN000133": "75279",
    "LOMIN000134": "84633",
    "LOMIN000135": "75376",
    "LOMIN000136": "75437",
    "LOMIN000137": "76133",
    "LOMIN000138": "76545",
    "LOMIN000139": "76546",
    "LOMIN000140": "76558",
    "LOMIN000141": "76559",
    "LOMIN000142": "65589",
    "LOMIN000143": "76703",
    "LOMIN000144": "76737",
    "LOMIN000145": "77435",
    "LOMIN000146": "77436",
    "LOMIN000147": "77437",
    "LOMIN000148": "77438",
    "LOMIN000149": "77581",
    "LOMIN000150": "77612",
    "LOMIN000151": "77613",
    "LOMIN000152": "77712",
    "LOMIN000153": "78310",
    "LOMIN000154": "78319",
    "LOMIN000155": "78362",
    "LOMIN000156": "78363",
    "LOMIN000157": "78715",
    "LOMIN000158": "79731",
    "LOMIN000159": "79842",
    "LOMIN000160": "80430",
    "LOMIN000161": "80808",
    "LOMIN000162": "83679",
    "LOMIN000163": "83863",
    "LOMIN000164": "80947",
    "LOMIN000165": "81011",
    "LOMIN000166": "81611",
    "LOMIN000167": "81868",
    "LOMIN000168": "84434",
    "LOMIN000169": "81976",
    "LOMIN000170": "82050",
    "LOMIN000171": "82159",
    "LOMIN000172": "82171",
    "LOMIN000173": "82205",
    "LOMIN000174": "82206",
    "LOMIN000175": "82207",
    "LOMIN000176": "82333",
    "LOMIN000177": "82334",
    "LOMIN000178": "82518",
    "LOMIN000179": "82574",
    "LOMIN000180": "82976",
    "LOMIN000181": "82978",
    "LOMIN000182": "83155",
    "LOMIN000183": "84778",
    "LOMIN000184": "82677",
    "LOMIN000185": "74288",
    "LOMIN000186": "70584",
    "LOMIN000187": "84111",
    "LOMIN000188": "65634",
    "LOMIN000189": "65517",
    "LOMIN000190": "65528",
    "LOMIN000191": "65537",
    "LOMIN000192": "65593",
    "LOMIN000193": "65594",
    "LOMIN000194": "65613",
    "LOMIN000195": "65621",
    "LOMIN000196": "65625",
    "LOMIN000197": "65646",
    "LOMIN000198": "65679",
    "LOMIN000199": "65738",
    "LOMIN000200": "65739",
    "LOMIN000201": "65740",
    "LOMIN000202": "67494",
    "LOMIN000203": "67497",
    "LOMIN000204": "67516",
    "LOMIN000205": "67645",
    "LOMIN000206": "67650",
    "LOMIN000207": "67869",
    "LOMIN000208": "83810",
    "LOMIN000209": "84110",
    "LOMIN000210": "84112",
    "LOMIN000211": "69540",
    "LOMIN000212": "83954",
    "LOMIN000213": "83966",
    "LOMIN000214": "88344",
    "LOMIN000215": "83945",
    "LOMIN000216": "68374",
    "LOMIN000217": "68832",
    "LOMIN000218": "68833",
    "LOMIN000219": "68662",
    "LOMIN000220": "71091",
    "LOMIN000221": "70795",
    "LOMIN000222": "76476",
    "LOMIN000223": "68681",
    "LOMIN000224": "68688",
    "LOMIN000225": "68689",
    "LOMIN000226": "68691",
    "LOMIN000227": "84165",
    "LOMIN000228": "68697",
    "LOMIN000229": "68699",
    "LOMIN000230": "68701",
    "LOMIN000231": "68702",
    "LOMIN000232": "68710",
    "LOMIN000233": "68703",
    "LOMIN000234": "68712",
    "LOMIN000235": "68704",
    "LOMIN000236": "68714",
    "LOMIN000237": "70791",
    "LOMIN000238": "68678",
    "LOMIN000239": "70788",
    "LOMIN000240": "70792",
    "LOMIN000241": "70793",
    "LOMIN000242": "68663",
    "LOMIN000243": "68727",
    "LOMIN000244": "76473",
    "LOMIN000245": "68729",
    "LOMIN000246": "68731",
    "LOMIN000247": "71374",
    "LOMIN000248": "76475",
    "LOMIN000249": "68740",
    "LOMIN000250": "70790",
    "LOMIN000251": "70595",
    "LOMIN000252": "70594",
    "LOMIN000253": "68748",
    "LOMIN000254": "68746",
    "LOMIN000255": "68751",
    "LOMIN000256": "68767",
    "LOMIN000257": "68766",
    "LOMIN000258": "68768",
    "LOMIN000259": "68754",
    "LOMIN000260": "68753",
    "LOMIN000261": "70561",
    "LOMIN000262": "68757",
    "LOMIN000263": "68890",
    "LOMIN000264": "71229",
    "LOMIN000265": "68721",
    "LOMIN000266": "65508",
    "LOMIN000267": "70818",
    "LOMIN000268": "84113",
    "LOMIN000269": "84114",
    "LOMIN000270": "84271",
    "LOMIN000271": "84596",
    "LOMIN000272": "84634",
    "LOMIN000273": "84777",
    "LOMIN000274": "85103",
    "LOMIN000275": "85342",
    "LOMIN000276": "85343",
    "LOMIN000277": "85457",
    "LOMIN000278": "85689",
    "LOMIN000279": "85709",
    "LOMIN000280": "85934",
    "LOMIN000281": "85995",
    "LOMIN000282": "86002",
    "LOMIN000283": "86008",
    "LOMIN000284": "86419",
    "LOMIN000285": "86420",
    "LOMIN000286": "86449",
    "LOMIN000287": "86510",
    "LOMIN000288": "86521",
    "LOMIN000289": "86536",
    "LOMIN000290": "86537",
    "LOMIN000291": "86538",
    "LOMIN000292": "86633",
    "LOMIN000293": "86699",
    "LOMIN000294": "86737",
    "LOMIN000295": "86999",
    "LOMIN000296": "87002",
    "LOMIN000297": "87005",
    "LOMIN000298": "87006",
    "LOMIN000299": "87007",
    "LOMIN000300": "87008",
    "LOMIN000301": "87123",
    "LOMIN000302": "87125",
    "LOMIN000303": "87164",
    "LOMIN000304": "87179",
    "LOMIN000305": "87180",
    "LOMIN000306": "87181",
    "LOMIN000307": "87182",
    "LOMIN000308": "87289",
    "LOMIN000309": "87290",
    "LOMIN000310": "87333",
    "LOMIN000311": "87334",
    "LOMIN000312": "87337",
    "LOMIN000313": "87338",
    "LOMIN000314": "87341",
    "LOMIN000315": "87392",
    "LOMIN000316": "87431",
    "LOMIN000317": "87498",
    "LOMIN000318": "87720",
    "LOMIN000319": "87777",
    "LOMIN000320": "87813",
    "LOMIN000321": "87923",
    "LOMIN000322": "87924",
    "LOMIN000323": "87925",
    "LOMIN000324": "88208",
    "LOMIN000325": "88063",
    "LOMIN000326": "88034",
    "LOMIN000327": "89148",
    "LOMIN000328": "88732",
    "LOMIN000329": "88343",
    "LOMIN000330": "89141",
    "LOMIN000331": "88346",
    "LOMIN000332": "88915",
    "LOMIN000334": "88911",
    "LOMIN000336": "88913",
    "LOMIN000337": "88914",
    "LOMIN000338": "91017",
    "LOMIN000339": "69664",
    "LOMIN000342": "89780",
    "LOMIN000343": "89731",
    "LOMIN000344": "89514",
    "LOMIN000345": "89516",
    "LOMIN000346": "89035",
    "LOMIN000347": "89473",
    "LOMIN000348": "89343",
    "LOMIN000349": "89347",
    "LOMIN000350": "89959",
    "LOMIN000352": "89810",
    "LOMIN000357": "68379",
    "LOMIN000359": "90139",
    "LOMIN000360": "90501",
    "LOMIN000365": "89472",
    "LOMIN000367": "89909",
    "LOMIN000368": "91018",
    "LOMIN000546": "91019",
    "LOMIN000548": "91037",
    "LOMIN000549": "91020",
    "LOMIN000551": "91038",
    "MCALI000481": "82397",
    "MCALI000589": "91009",
    "PEFLCANMI000001": "91127",
    "PEFLCANMI000002": "91128",
    "PEFLCANMI000006": "91388",
    "PEFLCANMI000008": "91279",
    "PEFLCANMI000011": "91288",
    "PEFLCANMI000012": "91352",
    "PEFLCANMI000013": "91353",
    "PEPOCANMI000001": "91129",
    "SUBCANMI000003": "1208",
    "SUBCANMI000005": "31814",
    "SUBCANMI000008": "59648",
    "SUBCANMI000009": "83205",
    "SUBCANMI000010": "85910",
    "SUBCOPMI000006": "31694",
    "SUBGOLMI000001": "53274",
    "SUBMIFMI000003": "33977",
    "SUBMIFMI000004": "33978",
    "SUBMIFMI000005": "34965",
    "SUBMIFMI000006": "89083",
    "SUBRACMI000001": "60071",
    "SUBRACMI000002": "60072",
    "SUBRACMI000003": "60073",
    "SUBRACMI000004": "70294",
    "SUBRACMI000005": "70267",
    "SUBRACMI000006": "70296",
    "SUBRACMI000007": "70295",
    "SUBRACMI000008": "75255",
    "SUBRACMI000009": "75256",
    "SUBRACMI000010": "75257",
    "SUBRACMI000011": "75258",
    "SUBRACMI000012": "75259",
    "SUBRACMI000013": "75260",
    "SUBRACMI000014": "75261",
    "SUBRACMI000015": "75262",
    "SUBRACMI000016": "70266",
    "SUBRACMI000017": "60069",
    "SUBRACMI000018": "12102",
    "SUBRACMI000019": "50275",
    "SUBRACMI000020": "12105",
    "SUBRACMI000021": "12106",
    "SUBRACMI000022": "12104",
    "SUBRACMI000023": "15932",
    "SUBRACMI000024": "12103",
    "SUBRACMI000025": "17546",
    "SUBRACMI000026": "12100",
    "SUBRACMI000028": "79424",
    "SUBRACMI000029": "80333",
    "SUBRACMI000033": "58766",
}


# ── Onde criar um card novo quando o código não existe ainda no HTML ──
# Prefixos sem ambiguidade: sempre vão para o mesmo lugar, não importa o texto.
# Não existe prefixo de código que garanta a seção sozinho - até COMPRO
# tem itens de Alimentos Preparados (ex: bolinhos, empanados) E itens de
# Congelados (ex: galinha/galo inteiro). A classificação é sempre feita
# lendo a descrição do produto, em classificar_produto_novo() abaixo.


def _sem_acento(txt):
    import unicodedata
    return "".join(
        c for c in unicodedata.normalize("NFD", txt)
        if unicodedata.category(c) != "Mn"
    )


def classificar_produto_novo(nome_produto):
    """Decide em qual seção/grupo do painel um produto novo deve entrar,
    lendo palavras-chave do nome/descrição do ERP. A descrição é o único
    sinal confiável - o prefixo/código NÃO indica a seção (ex: COMPRO tem
    tanto "Galinha Inteira Congelada" -> Congelados/Inteiros quanto
    "Sassami Empanado" -> Alimentos Preparados).
    Retorna (secao, grupo_label) ou None se não conseguir decidir com
    segurança (nesse caso o script só avisa no log, não cria o card)."""
    nome = _sem_acento(nome_produto or "").upper()

    # Alimentos Preparados: itens processados/prontos, mesmo que a
    # descrição também diga "CONGELADO" (ex: batata congelada, empanados).
    # Esse sinal tem prioridade sobre o corte de frango, porque um "Fut
    # Chicken" ou uma "Lasanha" não são pedaços de frango in natura.
    sinais_alimentos = (
        "EMPANAD", "CHICKEN", "BATATA", "MANDIOCA", "POLENTA",
        "ANEL DE CEBOLA", "PAO DE QUEIJO", "LASANHA", "TILAPIA", "PEIXE",
        "FISH", "BURGUER", "BOLINHO", "WINGS", "NUGGET", "ISCA DE",
        "HAMBURGUER", "STEAK", "NOISETTE", "KIBE", "QUIBE", "ESPETO",
        "GOLDEN", "PIZZA", "TORTA", "PASTEL", "COXINHA DE FRANGO PRE FRITA",
    )
    if any(k in nome for k in sinais_alimentos):
        return ("alimentos", "ALIMENTOS")

    if "IQF" in nome:
        return ("iqf", "IQF")

    if "RESFR" in nome:
        grupo = "BANDEJA" if ("BANDEJA" in nome or "BDJ" in nome) else "PACOTE"
        return ("resfriados", grupo)

    if "CONG" in nome:
        eh_bandeja = "BANDEJA" in nome or "BDJ" in nome
        if eh_bandeja:
            return ("congelados", "GRUPO BANDEJA CONGELADA")
        if any(k in nome for k in ("INTEIR", "GALINHA", "GALO", "CARCACA", "TEMPERAD")):
            return ("congelados", "GRUPO INTEIROS")
        # "FRANGO CONGELADO C/ MIUDOS (2 FIGADO 2 PESCOCO)" é a ave inteira
        # (com ou sem os miúdos dentro), não um miúdo avulso - sem esta
        # regra as palavras FIGADO/PESCOCO/MIUDOS jogavam ele em MIÚDOS.
        if "FRANGO" in nome and re.search(r"\b(C/|S/|COM|SEM)\s*MIUDO", nome):
            return ("congelados", "GRUPO INTEIROS")
        if any(k in nome for k in ("ASA", "DRUMET")):
            return ("congelados", "GRUPO ASA")
        if any(k in nome for k in ("COXA", "SOBRECOXA", "PERNA", "DORSAL",
                                    "LEG QUARTER", "TULIPA", "QUARTO DE PERNA")):
            return ("congelados", "GRUPO PERNA")
        if any(k in nome for k in ("PEITO", "FILE", "FILEZINHO", "SASSAMI")):
            return ("congelados", "GRUPO PEITO")
        if any(k in nome for k in ("CORACAO", "FIGADO", "MOELA", "PESCOCO", "CMS",
                                    "PES ", "SAMBIQUIRA", "MIUDO", "CARTILAGEM",
                                    "MOLA", "PAPADA", "CRISTA", "MIUDEZA")):
            return ("congelados", "GRUPO MIUDOS")
        # é congelado mas o corte especifico nao bateu com nenhuma palavra-chave
        return None

    return None


# ── Seções/grupos válidos - usados tanto pelas regras de palavra-chave acima
# quanto pelo fallback de IA abaixo, pra nunca inventar um grupo que não
# existe de fato no painel. ──
SECOES_VALIDAS = {
    "resfriados": ["BANDEJA", "PACOTE"],
    "congelados": [
        "GRUPO ASA", "GRUPO BANDEJA CONGELADA", "GRUPO INTEIROS",
        "GRUPO MIUDOS", "GRUPO PEITO", "GRUPO PERNA",
    ],
    "iqf": ["IQF"],
    "alimentos": ["ALIMENTOS"],
}


def classificar_com_ia(nome_produto):
    """Fallback quando classificar_produto_novo() não consegue decidir por
    palavra-chave: manda a descrição do produto pra Claude e pede pra
    classificar, restrito às combinações de seção+grupo que já existem de
    fato no painel (SECOES_VALIDAS acima).

    Só roda se a secret ANTHROPIC_API_KEY estiver configurada no repositório.
    Qualquer situação que não dê 100% de segurança - sem chave configurada,
    erro de rede, resposta fora do formato esperado, ou o próprio modelo
    dizendo que não tem certeza - devolve None, e quem chamou (inserir_cards_novos)
    cai de volta pra seção 'Não Classificados' normalmente. Isso nunca trava
    nem atrasa a atualização do estoque em si."""
    api_key = os.environ.get("ANTHROPIC_API_KEY")
    if not api_key or not (nome_produto or "").strip():
        return None

    opcoes = "\n".join(
        f'- secao="{secao}", grupo="{grupo}"'
        for secao, grupos in SECOES_VALIDAS.items()
        for grupo in grupos
    )
    prompt = (
        "Você classifica produtos do estoque de um frigorífico (frango, "
        "peixe e alimentos preparados) em uma das combinações de seção+grupo "
        "abaixo, com base só na descrição do produto (texto cru do ERP, "
        "normalmente em maiúsculas e abreviado).\n\n"
        f"Combinações válidas (use exatamente uma delas):\n{opcoes}\n\n"
        f'Descrição do produto: "{nome_produto}"\n\n'
        "Responda SOMENTE com um JSON, sem nenhum texto antes ou depois, "
        'no formato {"secao": "...", "grupo": "..."}. Se não tiver certeza '
        'suficiente, responda {"secao": null, "grupo": null}.'
    )

    try:
        resp = requests.post(
            "https://api.anthropic.com/v1/messages",
            headers={
                "x-api-key": api_key,
                "anthropic-version": "2023-06-01",
                "content-type": "application/json",
            },
            json={
                "model": "claude-haiku-4-5",
                "max_tokens": 100,
                "messages": [{"role": "user", "content": prompt}],
            },
            timeout=20,
        )
        resp.raise_for_status()
        texto = resp.json()["content"][0]["text"].strip()
        m = re.search(r"\{.*\}", texto, re.S)
        if not m:
            return None
        obj = json.loads(m.group(0))
        secao, grupo = obj.get("secao"), obj.get("grupo")
        if secao in SECOES_VALIDAS and grupo in SECOES_VALIDAS[secao]:
            return (secao, grupo)
        return None
    except Exception as e:
        print(f"AVISO - classificação por IA falhou pra '{nome_produto}': {e}")
        return None



def baixar_csv(url):
    # Pede versão fresca: o CSV "publicado na web" do Google fica em cache por
    # alguns minutos, e como essa rodada normalmente começa poucos segundos
    # depois da planilha ser editada, sem isso é fácil ler a versão anterior
    # (sintoma: "editei a planilha e o site não mudou nada" - e aí a mudança
    # só aparece na rodada seguinte, de hora em hora).
    resp = requests.get(url, headers={
        "Cache-Control": "no-cache, max-age=0",
        "Pragma": "no-cache",
    }, timeout=60)
    resp.raise_for_status()
    resp.encoding = "utf-8"
    return resp.text


def carregar_validades(csv_text):
    """Lê a aba de validades (Cód. Prod. Cliente, Peso Líquido, Validade)
    e monta o mesmo formato usado no VALIDADES do HTML:
    { codigo: { entries: [{d, p, v}], hv: bool } }
    """
    reader = csv.reader(StringIO(csv_text))
    rows = list(reader)
    hoje = datetime.now(timezone.utc) - timedelta(hours=3)
    hoje = hoje.replace(hour=0, minute=0, second=0, microsecond=0, tzinfo=None)

    validades = {}
    for r in rows[2:]:
        if not r or not r[0]:
            continue
        codigo = r[0].strip()
        try:
            peso = float(str(r[1]).replace(".", "").replace(",", ".")) if r[1] else 0
        except (ValueError, IndexError):
            peso = 0
        data_str = r[2].strip() if len(r) > 2 else ""
        if not data_str:
            continue
        try:
            data_venc = datetime.strptime(data_str, "%d/%m/%Y")
        except ValueError:
            continue
        vencido = data_venc < hoje
        entry = {"d": data_str, "p": peso, "v": vencido}
        validades.setdefault(codigo, {"entries": [], "hv": False})
        validades[codigo]["entries"].append(entry)
        if vencido:
            validades[codigo]["hv"] = True
    return validades


def carregar_saldos(csv_text):
    """Retorna tanto os saldos quanto os dados brutos (nome, tp) de cada
    código, para permitir criar cards novos quando necessário."""
    reader = csv.reader(StringIO(csv_text))
    rows = list(reader)
    data = {}
    linhas_brutas = {}
    # linha 0 = título da planilha, linha 1 = cabeçalho, dados a partir da linha 2
    for r in rows[2:]:
        if not r or not r[0]:
            continue
        codigo = r[0].strip()
        try:
            disponivel = float(str(r[9]).replace(".", "").replace(",", ".")) if r[9] else 0
        except (ValueError, IndexError):
            disponivel = 0
        data[codigo] = disponivel
        linhas_brutas[codigo] = r
    return data, linhas_brutas


def _parse_num_br(txt):
    """Converte um número no formato brasileiro (2.250 = dois mil e
    duzentos e cinquenta; 2,25 = dois vírgula vinte e cinco) pra float.
    Retorna None se estiver vazio/ilegível - o front trata isso como "-"."""
    txt = (txt or "").strip()
    if not txt:
        return None
    try:
        return float(txt.replace(".", "").replace(",", "."))
    except ValueError:
        return None


def _data_para_ordenacao(data_str):
    """Linhas sem data de carregamento (ex: "PROGRAMADA" sem data ainda
    definida) vão pro fim da lista - as demais ficam em ordem cronológica."""
    try:
        return datetime.strptime((data_str or "").strip(), "%d/%m/%Y")
    except ValueError:
        return datetime.max


def carregar_programacao_cargas(csv_text):
    """Lê a planilha de programação de cargas (publicada como CSV) e agrupa
    as linhas por Código Protheus do produto - é isso que alimenta o botão
    "🚚 programação" de cada card do estoque, mostrando só as cargas
    daquele produto específico.

    Duas particularidades da planilha original (confirmadas com um export
    real dela) que essa função precisa tratar:

    1. Pode ter uma linha em branco antes do cabeçalho de verdade - por
       isso o cabeçalho é localizado procurando a linha que contém
       "CÓDIGO PROTHEUS", em vez de assumir que é sempre a linha 1.
    2. Os dados de cada "viagem" (FILIAL, DATA CARREGAMENTO, VIAGEM_MAE,
       STATUS, NF, DATA/HORA FAT., PLACAS, PEDIDO) só vêm preenchidos na
       primeira linha do carregamento; nas linhas seguintes (outros
       produtos da mesma carga) essas colunas ficam em branco - é o
       "preenche uma vez só" comum de planilha feita à mão. Por isso essas
       colunas são propagadas (forward-fill) da última linha em que
       apareceram preenchidas. GRUPO/EMBALAGEM, LOCAL DE EMBARQUE, CÓDIGO,
       CÓDIGO PROTHEUS, PRODUTO, Kg PROGR. e Kg FAT. sempre vêm
       preenchidas linha a linha (não são propagadas).
    """
    linhas_csv = list(csv.reader(StringIO(csv_text)))

    def _norm(h):
        return _sem_acento((h or "").strip().upper())

    idx_cabecalho = None
    for i, r in enumerate(linhas_csv):
        if any(_norm(c) in ("CODIGO PROTHEUS",) for c in r):
            idx_cabecalho = i
            break
    if idx_cabecalho is None:
        return {}, {}

    cabecalho = linhas_csv[idx_cabecalho]
    col_idx = {_norm(h): i for i, h in enumerate(cabecalho)}

    def _idx(*nomes):
        for nome in nomes:
            i = col_idx.get(_norm(nome))
            if i is not None:
                return i
        return None

    idx_data = _idx("DATA CARREGAMENTO")
    idx_viagem_mae = _idx("VIAGEM_MAE")
    idx_status = _idx("STATUS")
    idx_nf = _idx("NF")
    idx_data_hora_fat = _idx("DATA/HORA FAT.", "DATA/HORA FAT")
    idx_placas = _idx("PLACAS")
    idx_pedido = _idx("PEDIDO")
    idx_codigo_protheus = _idx("CÓDIGO PROTHEUS", "CODIGO PROTHEUS")
    idx_produto = _idx("PRODUTO")
    idx_kg_prog = _idx("KG PROGR.", "KG PROGR", "KG PROG.", "KG PROG")
    idx_kg_fat = _idx("KG FAT.", "KG FAT")

    def _cel(r, i):
        if i is None or i >= len(r):
            return ""
        return (r[i] or "").strip()

    # Propaga (forward-fill) os campos que descrevem a viagem/carga em si -
    # na planilha eles só vêm preenchidos na primeira linha de cada
    # carregamento; nas linhas seguintes (outros produtos da mesma carga)
    # ficam em branco e herdam o último valor visto.
    carry_data = carry_viagem_mae = carry_status = ""
    # PLACAS/PEDIDO/NF/DATA-HORA FAT. só são herdados por linha de
    # CONTINUAÇÃO PURA - aquela que só traz produto e quilos, sem trazer
    # nenhuma informação própria de carga. Duas situações resetam esses
    # campos (passando a valer só o que a própria linha traz, mesmo que
    # vazio):
    #   1. nova viagem: linha com DATA CARREGAMENTO ou VIAGEM_MAE preenchidos;
    #   2. status próprio: linha que traz o STATUS dela mesma, ou seja, um
    #      bloco de itens em situação diferente dentro do mesmo carregamento
    #      (caso real: itens "PROGRAMADA" listados dentro de uma carga já
    #      "EM TRÂNSITO" - esses ainda não têm NF, e herdar a NF da carga
    #      passaria a impressão errada de que já foram faturados).
    # Assim os outros produtos da mesma carga faturada mostram a NF que
    # realmente é deles (antes apareciam com "-", o que fazia a carga
    # parecer sem nota), sem inventar NF pra quem ainda não tem.
    carry_placas = carry_pedido = carry_nf = carry_fat = ""

    programacao = {}
    nomes_produto = {}
    for r in linhas_csv[idx_cabecalho + 1:]:
        if not any(c.strip() for c in r):
            continue  # linha totalmente em branco entre grupos, se houver

        codigo = _cel(r, idx_codigo_protheus)
        if not codigo:
            continue

        val_data = _cel(r, idx_data)
        val_viagem_mae = _cel(r, idx_viagem_mae)
        val_status = _cel(r, idx_status)
        nova_viagem = bool(val_data or val_viagem_mae)
        # Linha que traz informação própria de carga (nova viagem ou status
        # próprio) zera o que era herdado - o que valer daqui pra frente é
        # o que essa linha declarar.
        if nova_viagem or val_status:
            carry_placas = carry_pedido = carry_nf = carry_fat = ""

        if val_data:
            carry_data = val_data
        if val_viagem_mae:
            carry_viagem_mae = val_viagem_mae
        if val_status:
            carry_status = val_status
        if _cel(r, idx_placas):
            carry_placas = _cel(r, idx_placas)
        if _cel(r, idx_pedido):
            carry_pedido = _cel(r, idx_pedido)
        if _cel(r, idx_nf):
            carry_nf = _cel(r, idx_nf)
        if _cel(r, idx_data_hora_fat):
            carry_fat = _cel(r, idx_data_hora_fat)

        entrada = {
            "data": carry_data,
            "status": carry_status,
            "nf": carry_nf,
            "placas": carry_placas,
            "pedido": carry_pedido,
            "kgProg": _parse_num_br(_cel(r, idx_kg_prog)),
            "kgFat": _parse_num_br(_cel(r, idx_kg_fat)),
        }
        programacao.setdefault(codigo, []).append(entrada)
        if codigo not in nomes_produto:
            nome_produto = _cel(r, idx_produto)
            if nome_produto:
                nomes_produto[codigo] = nome_produto

    for linhas in programacao.values():
        linhas.sort(key=lambda l: _data_para_ordenacao(l["data"]))

    return programacao, nomes_produto


def fmt_kg(v):
    if v is None:
        v = 0
    neg = v < 0
    v = abs(v)
    if v == int(v):
        s = f"{int(v):,}".replace(",", ".")
    else:
        s = f"{v:,.1f}".replace(",", "X").replace(".", ",").replace("X", ".")
    return ("-" if neg else "") + s + " kg"


def slug_nome(nome):
    """Nome do produto em Title Case simples, a partir da Descricao do ERP
    (que normalmente vem toda em maiúsculas)."""
    nome = nome.strip()
    palavras_minusculas = {"de", "da", "do", "das", "dos", "e", "c/", "s/", "a", "com"}
    partes = nome.split(" ")
    out = []
    for i, p in enumerate(partes):
        if i > 0 and p.lower() in palavras_minusculas:
            out.append(p.lower())
        else:
            out.append(p.capitalize() if p.isupper() or p.islower() else p)
    return " ".join(out)


_TAG_RE = re.compile(r"<div\b|</div>")


def _find_matching_div_end(html, div_start):
    """Dado o índice onde começa uma tag <div ...>, encontra o índice logo
    após o </div> que fecha ela (contando divs aninhadas). Retorna -1 se
    não achar (HTML malformado)."""
    primeiro_fechamento_tag = html.find(">", div_start)
    if primeiro_fechamento_tag == -1:
        return -1
    pos = primeiro_fechamento_tag + 1
    profundidade = 1
    for m in _TAG_RE.finditer(html, pos):
        if m.group(0) == "<div":
            profundidade += 1
        else:
            profundidade -= 1
            if profundidade == 0:
                return m.end()
    return -1


def montar_card_novo_html(codigo, nome_produto, peso_cx, cod_cancao, saldo, secao):
    """Monta o HTML de um card novo, no mesmo formato dos existentes.
    O nome exibido prioriza NOME_CANONICO (fixado manualmente); só usa a
    descrição bruta do ERP, formatada, quando o código não está mapeado."""
    if codigo in NOME_CANONICO:
        nome_fmt = NOME_CANONICO[codigo]
    else:
        nome_fmt = slug_nome(nome_produto)
    nome_esc = nome_fmt.replace('"', "&quot;")
    status = "available" if saldo and saldo > 0 else "no-stock"
    stock_html = (
        f'<div class="card-stock"><div class="stock-kg">{fmt_kg(saldo)}</div></div>'
        if status == "available" else
        '<div class="card-stock"><div class="no-stock-tag">SEM ESTOQUE</div></div>'
    )
    peso_txt = f"Cx {peso_cx} kg" if peso_cx else ""
    cancao_txt = cod_cancao if cod_cancao else "—"
    # "new-item" marca o card como recém-criado automaticamente, pro painel
    # destacar ele visualmente e oferecer o botão de renomear. Fica marcado
    # até alguém dispensar no navegador (client-side) - o script preserva
    # essa classe nas próximas atualizações (ver atualizar_html).
    return (
        f'<div class="card {status} new-item" data-name="{nome_esc}" data-section="{secao}" data-status="{status}">'
        f'<div class="card-info"><div class="card-name">{nome_esc}</div>'
        f'<div class="card-code">{codigo} · {peso_txt}</div>'
        f'<div class="card-cancao">Cód. Canção: {cancao_txt}</div></div>'
        f'{stock_html}</div>\n      '
    )


def inserir_cards_novos(html, data, linhas_brutas, programacao=None, nomes_produto_programacao=None):
    """Para códigos que estão na planilha mas não têm card nenhum no HTML,
    cria o card automaticamente. Prefixos fixos (COMPRO, PEFLCANMI) vão
    direto para Alimentos Preparados; os demais são classificados pelo
    texto da descrição do produto (RESFRIADA/CONGELADA/IQF + tipo de
    corte). Só fica sem criar quando a descrição não dá pra classificar
    com segurança - nesse caso o script avisa no log.

    Também cria um card "SEM ESTOQUE" pra qualquer código que apareça na
    planilha de programação de cargas mas não tenha estoque cadastrado
    (produto zerado/descontinuado, mas com carga programada vindo) - assim
    sempre existe um card clicável com o botão "🚚 programação", mesmo sem
    saldo nenhum."""
    programacao = programacao or {}
    nomes_produto_programacao = nomes_produto_programacao or {}

    codigos_existentes = set(re.findall(r'card-code">([^<· ]+)', html))

    codigos_novos = [c for c in data.keys() if c not in codigos_existentes]
    codigos_somente_programacao = [
        c for c in programacao.keys()
        if c not in codigos_existentes and c not in data
    ]
    if not codigos_novos and not codigos_somente_programacao:
        return html, []

    avisos = []
    inseridos = []

    itens = [(c, linhas_brutas.get(c, [])[3] if len(linhas_brutas.get(c, [])) > 3 else c, data.get(c, 0))
             for c in codigos_novos]
    itens += [(c, nomes_produto_programacao.get(c, c), 0) for c in codigos_somente_programacao]

    for codigo, nome_produto, saldo in itens:
        destino = classificar_produto_novo(nome_produto)

        if destino is None:
            # 1º fallback: pede pra IA (Claude) classificar pela descrição,
            # restrito às seções/grupos que realmente existem no painel.
            destino = classificar_com_ia(nome_produto)
            if destino is not None:
                avisos.append(
                    f"código novo '{codigo}' ({nome_produto}) não bateu com "
                    f"nenhuma palavra-chave conhecida - classificado por IA "
                    f"em '{destino[0]}/{destino[1]}'."
                )

        if destino is None:
            # 2º fallback (rede de segurança final): antes o item era
            # descartado aqui (nenhum card criado, só um aviso no log do
            # Actions que ninguém via) - por isso itens novos "sumiam" do
            # painel sem explicação. Agora, mesmo sem conseguir classificar
            # nem por palavra-chave nem por IA, o card é sempre criado, na
            # seção "Não Classificados" - fica visível no painel e só
            # precisa ser movido manualmente pra seção certa quando der.
            destino = ("outros", "NÃO CLASSIFICADO")
            avisos.append(
                f"código novo '{codigo}' ({nome_produto}) não bateu com "
                f"nenhuma palavra-chave conhecida e a IA não teve certeza (ou "
                f"não está configurada) - card criado em 'Não Classificados', "
                f"mova manualmente pra seção certa quando puder."
            )

        secao, grupo_label = destino
        # A descrição do ERP quase sempre tem "CX PP <peso> KG" (às vezes só
        # "CX <peso> KG") - o "PP"/"PV"/etc. no meio fazia o peso não bater
        # e o card novo nascer sem o "Cx X kg" no cabeçalho.
        m_peso = re.search(r"CX\s+(?:[A-ZÇÃÕ]+\s+)?([\d,.]+)\s*KG", nome_produto, re.I)
        peso_cx = m_peso.group(1).replace(".", ",") if m_peso else ""

        cod_cancao = COD_CANCAO_FIXO.get(codigo, "")
        card_html = montar_card_novo_html(codigo, nome_produto, peso_cx, cod_cancao, saldo, secao)

        # Acha a seção certa, depois o group-label certo DENTRO dela, depois
        # a div "cards" logo em seguida - usando contagem de divs balanceada
        # (regex simples falha com divs aninhadas do mesmo nome).
        inserido_ok = False
        secao_start = html.find(f'data-section="{secao}"')
        if secao_start != -1:
            # recua até o "<div class="section"" que contém esse data-section
            secao_div_start = html.rfind('<div class="section"', 0, secao_start)
            secao_end = _find_matching_div_end(html, secao_div_start)
            if secao_end != -1:
                bloco_secao = html[secao_div_start:secao_end]
                grupo_idx = bloco_secao.find(f'<div class="group-label">{grupo_label}</div>')
                if grupo_idx != -1:
                    cards_div_start = bloco_secao.find('<div class="cards">', grupo_idx)
                    if cards_div_start != -1:
                        cards_div_end = _find_matching_div_end(bloco_secao, cards_div_start)
                        if cards_div_end != -1:
                            # cards_div_end aponta logo apos o "</div>" de fechamento;
                            # insere o novo card imediatamente antes desse "</div>".
                            ponto_insercao = cards_div_end - len("</div>")
                            novo_bloco_secao = (
                                bloco_secao[:ponto_insercao]
                                + card_html
                                + bloco_secao[ponto_insercao:]
                            )
                            html = html[:secao_div_start] + novo_bloco_secao + html[secao_end:]
                            inserido_ok = True

        if inserido_ok:
            inseridos.append(codigo)
        else:
            avisos.append(
                f"código novo '{codigo}' seria criado em '{secao}/{grupo_label}' "
                f"mas não encontrei esse grupo no HTML - card NÃO foi criado."
            )

    for a in avisos:
        print("AVISO -", a)
    if inseridos:
        print(f"OK - {len(inseridos)} card(s) novo(s) criado(s) automaticamente: {', '.join(inseridos)}")

    return html, inseridos


def atualizar_html(html, data, validades=None, linhas_brutas=None, programacao=None, nomes_produto_programacao=None):
    validades = validades or {}
    linhas_brutas = linhas_brutas or {}
    programacao = programacao or {}
    nomes_produto_programacao = nomes_produto_programacao or {}

    # ── Cria cards novos ANTES de atualizar os existentes, para que o saldo
    # do card recém-criado já seja processado no mesmo ciclo. Isso inclui
    # tanto itens genuinamente novos no estoque quanto itens que só têm
    # programação de carga (sem estoque cadastrado ainda/mais) - esses
    # últimos nascem "SEM ESTOQUE", só pra existir um card clicável com o
    # botão "🚚 programação". ──
    html, criados_agora = inserir_cards_novos(html, data, linhas_brutas, programacao, nomes_produto_programacao)
    # Códigos que nasceram NESTA rodada: são os únicos que mantêm o selo
    # "NOVO" quando o laço abaixo reescreve a classe do card. Qualquer card
    # que já existia perde o selo (ele vale por uma atualização só).
    criados_agora = set(criados_agora)

    card_starts = [m.start() for m in re.finditer(r'<div class="card[^"]*" data-name=', html)]
    card_starts.append(len(html))

    segments = []
    for i in range(len(card_starts) - 1):
        start, end = card_starts[i], card_starts[i + 1]
        # O "block" entre um card e o próximo pode conter, depois do
        # fechamento do card, HTML estrutural (fechamento de grupo/seção,
        # cabeçalho da próxima seção etc). Isolamos só o HTML do card em
        # si (via contagem balanceada de divs) do "rastro" que vem depois,
        # pra nunca apagar estrutura junto quando um card é removido.
        card_own_end = _find_matching_div_end(html, start)
        if card_own_end == -1 or card_own_end > end:
            card_own_end = end
        block = html[start:card_own_end]
        rastro = html[card_own_end:end]

        m = re.search(r'card-code">([^<]+)</div>', block)
        if not m:
            segments.append(block)
            segments.append(rastro)
            continue
        code = m.group(1).split("·")[0].strip()

        # ── Autocorreção: nome e Cód. Canção sempre seguem as listas fixas
        # (NOME_CANONICO / COD_CANCAO_FIXO), mesmo em cards que já existiam
        # antes dessas listas serem criadas - corrige uma vez e pra sempre. ──
        if code in NOME_CANONICO:
            nome_certo = NOME_CANONICO[code]
            nome_certo_esc = nome_certo.replace('"', "&quot;")
            block = re.sub(
                r'(<div class="card[^"]*" data-name=")[^"]*(")',
                lambda m2: m2.group(1) + nome_certo_esc + m2.group(2),
                block, count=1,
            )
            block = re.sub(
                r'(<div class="card-name">)[^<]*(</div>)',
                lambda m2: m2.group(1) + nome_certo_esc + m2.group(2),
                block, count=1,
            )
        if code in COD_CANCAO_FIXO:
            cancao_certo = COD_CANCAO_FIXO[code]
            block = re.sub(
                r'(<div class="card-cancao">Cód\. Canção: )[^<]*(</div>)',
                lambda m2: m2.group(1) + cancao_certo + m2.group(2),
                block, count=1,
            )

        # ── Gerencia o botão "ver validades" independente do estoque ──
        tem_validade = code in validades and validades[code].get("entries")
        tem_botao = bool(re.search(r'<button class="val-btn[^"]*"[^>]*data-code="' + re.escape(code) + r'"', block))
        if tem_validade and not tem_botao:
            hv = validades[code].get("hv", False)
            if hv:
                btn = f' <button class="val-btn val-btn-alert" onclick="toggleVal(this)" data-code="{code}">⚠️ ver validades</button>'
            else:
                btn = f' <button class="val-btn" onclick="toggleVal(this)" data-code="{code}">📅 ver validades</button>'
            novo_block = re.sub(
                r'(<div class="card-cancao">Cód\. Canção: [^<]*</div>)</div>',
                r'\1' + btn + '</div>', block, count=1,
            )
            if novo_block != block:
                block = novo_block
        elif not tem_validade and tem_botao:
            block = re.sub(r'\s*<button class="val-btn[^"]*"[^>]*>[^<]*</button>', '', block, count=1)
        elif tem_validade and tem_botao:
            # atualiza o estilo do botão (normal <-> alerta) se o hv mudou
            hv = validades[code].get("hv", False)
            nova_classe = "val-btn val-btn-alert" if hv else "val-btn"
            novo_texto = "⚠️ ver validades" if hv else "📅 ver validades"
            block = re.sub(
                r'<button class="val-btn[^"]*"(\s+onclick="toggleVal\(this\)" data-code="' + re.escape(code) + r'">)[^<]*</button>',
                f'<button class="{nova_classe}"' + r'\1' + novo_texto + '</button>',
                block, count=1,
            )

        # ── Gerencia o botão "🚚 programação" independente do estoque ──
        # (fica sempre depois de qualquer botão "ver validades" já tratado
        # acima - o anchor abaixo pega o fim de card-info seja lá o que
        # tiver dentro dele nesse ponto.)
        tem_prog = bool(programacao.get(code))
        tem_botao_prog = bool(re.search(r'<button class="prog-btn"[^>]*data-code="' + re.escape(code) + r'"', block))
        if tem_prog and not tem_botao_prog:
            btn_prog = f' <button class="prog-btn" onclick="toggleProg(this)" data-code="{code}">🚚 programação</button>'
            novo_block = re.sub(
                r'(<div class="card-info">.*?)(</div>\s*<div class="card-stock">)',
                lambda m2: m2.group(1) + btn_prog + m2.group(2),
                block, count=1, flags=re.S,
            )
            if novo_block != block:
                block = novo_block
        elif not tem_prog and tem_botao_prog:
            block = re.sub(r'\s*<button class="prog-btn"[^>]*>[^<]*</button>', '', block, count=1)

        if code not in data:
            if tem_prog:
                # Exceção: o produto saiu da planilha de estoque (acabou/
                # descontinuado), mas ainda tem programação de carga vindo -
                # em vez de apagar o card, mantém ele marcado "SEM ESTOQUE",
                # só pra continuar existindo um card clicável com o botão
                # "🚚 programação" (é só isso que sobra dele nesse caso).
                val = 0
            else:
                # Regra: se o código não aparece mais na planilha de estoque
                # nem tem programação pendente, o produto foi descontinuado/
                # removido do ERP - o card some do painel (não fica com o
                # saldo antigo parado). Se o código reaparecer numa planilha
                # futura, o card é recriado automaticamente. O "rastro"
                # (fechamentos de grupo/seção, próximo cabeçalho) é sempre
                # preservado, mesmo quando o card é removido.
                segments.append(rastro)
                continue
        else:
            val = data[code]
        was_nostock = 'data-status="no-stock"' in block
        classe_atual_m = re.search(r'<div class="card ([^"]*)" data-name=', block)
        tinha_has_obs = classe_atual_m and "has-obs" in classe_atual_m.group(1)
        # O selo "NOVO" (classe "new-item", ver montar_card_novo_html) vale
        # por UMA atualização só: fica apenas nos cards criados nesta rodada.
        # Card que já existia com o selo perde ele aqui - o produto apareceu
        # na atualização anterior, então não é mais novidade.
        tinha_new_item = code in criados_agora
        sufixo_new_item = " new-item" if tinha_new_item else ""
        if val and val > 0:
            block = re.sub(
                r'(<div class="card )[^"]*(" data-name="[^"]*" data-section="[^"]*" data-status=")[^"]*(")',
                rf"\1available{sufixo_new_item}\g<2>available\3", block, count=1,
            )
            block = re.sub(
                r'<div class="card-stock">.*?</div>\s*</div>',
                f'<div class="card-stock"><div class="stock-kg">{fmt_kg(val)}</div></div>',
                block, count=1, flags=re.S,
            )
            if was_nostock:
                block = re.sub(r'\s*<div class="obs-wrap">.*?</div>\s*', "\n", block, flags=re.S)
                block = block.replace(" has-obs", "").replace("has-obs ", "").replace("has-obs", "")
        else:
            partes_classe = ["no-stock"]
            if tinha_has_obs:
                partes_classe.append("has-obs")
            if tinha_new_item:
                partes_classe.append("new-item")
            classe_nova = " ".join(partes_classe)
            block = re.sub(
                r'(<div class="card )[^"]*(" data-name="[^"]*" data-section="[^"]*" data-status=")[^"]*(")',
                rf"\1{classe_nova}\g<2>no-stock\3", block, count=1,
            )
            block = re.sub(
                r'<div class="card-stock">.*?</div>\s*</div>',
                '<div class="card-stock"><div class="no-stock-tag">SEM ESTOQUE</div></div>',
                block, count=1, flags=re.S,
            )
        segments.append(block)
        segments.append(rastro)

    new_html = html[: card_starts[0]] + "".join(segments)

    # Atualiza os dados de validade usados pelos selos automáticos (vence em Xd / vencido)
    new_html = re.sub(
        r"var VALIDADES = (\{.*?\})\s*;",
        "var VALIDADES = " + json.dumps(validades, ensure_ascii=False) + ";",
        new_html, count=1, flags=re.S,
    )

    # Atualiza os dados usados pelo botão "🚚 programação" de cada card
    new_html = re.sub(
        r"var PROGRAMACAO = (\{.*?\})\s*;",
        "var PROGRAMACAO = " + json.dumps(programacao, ensure_ascii=False) + ";",
        new_html, count=1, flags=re.S,
    )

    # Atualiza o histórico de tendência (mantém últimos 4 ciclos)
    m = re.search(r"var HISTORICO = (\{.*?\})\s*;", new_html, re.S)
    if m:
        hist = json.loads(m.group(1))
        for code, val in data.items():
            arr = hist.get(code, [])
            arr.append(float(val))
            hist[code] = arr[-4:]
        new_hist_str = json.dumps(hist, ensure_ascii=False)
        new_html = new_html[: m.start(1)] + new_hist_str + new_html[m.end(1):]

    # Atualiza o horário exibido como "Atualizado hoje às HHhMM" (horário de Brasília, UTC-3)
    agora_brt = datetime.now(timezone.utc) - timedelta(hours=3)
    novo_timestamp = agora_brt.strftime("%Y-%m-%dT%H:%M:%S")
    new_html = re.sub(
        r'var ULTIMA_ATUALIZACAO = "[^"]*";',
        f'var ULTIMA_ATUALIZACAO = "{novo_timestamp}";',
        new_html,
    )

    # Atualiza também o selo de data no cabeçalho (badge-date), que é o campo
    # que decide se o painel mostra "hoje"/"ontem"/"há X dias"
    nova_data_badge = agora_brt.strftime("%d/%m/%Y")
    new_html = re.sub(
        r'<span class="badge-date">[^<]*</span>',
        f'<span class="badge-date">{nova_data_badge}</span>',
        new_html,
    )
    new_html = re.sub(
        r'<title>Estoque Apucarana – [^<]*</title>',
        f'<title>Estoque Apucarana – {nova_data_badge}</title>',
        new_html,
    )

    return new_html


def fmt_preco(v):
    v = (v or "").strip()
    if not v:
        return None
    # valores não numéricos (ex: "—") são mantidos como estão
    limpo = v.replace(".", "").replace(",", ".")
    try:
        num = float(limpo)
    except ValueError:
        return v
    return f"{num:.2f}".replace(".", ",")


def _normalizar_nome_preco(txt):
    """Normaliza um nome de produto pra comparação: tira acento, baixa a
    caixa, troca pontuação (/()., e apóstrofos) por espaço e colapsa
    espaços. Usada tanto pro NOME da planilha quanto pro data-name do card,
    porque um raramente é digitado igual ao outro (acento, barra, abreviação)."""
    nome = _sem_acento(txt or "").lower()
    nome = re.sub(r"[/().'’\"“”+\-]", " ", nome)
    nome = re.sub(r"\s+", " ", nome).strip()
    return nome


def _palavras_nome_preco(nome_normalizado):
    """Conjunto de palavras de um nome já normalizado, usado no pareamento
    por semelhança (ver _achar_info em atualizar_precos_html)."""
    return {p for p in re.split(r"[^a-z0-9,]+", nome_normalizado) if p}


# ── Mapa "Seção (referência)" (coluna da planilha) -> data-sec (do HTML) ──
# Sem esse mapa, o script não tinha como saber que "coxa pilão" na aba de
# Preços podia ser TRÊS produtos diferentes (bandeja resfriada, individual
# resfriada, ou pacote congelado normal) com preços diferentes - a versão
# antiga ignorava essa coluna, então quando o mesmo NOME aparecia em mais de
# uma seção, só a última linha lida "vencia" e as outras eram perdidas (por
# isso preços não batiam/não atualizavam).
SECAO_REF_PARA_DATA_SEC = {
    "cortes resfriados bandeja": "resfBdj",
    "cortes resfriados individual": "resfInd",
    "inteiros": "inteiros",
    "perna": "perna",
    "peito": "peito",
    "asa": "asa",
    "miudos": "miudos",
    "iqf s": "iqf",
    "iqf": "iqf",
    "mix alimentos preparados": "mix",
    "pescados": "pescados",
    "vegetais": "vegetais",
    "lancamentos copa": "copa",
}

# Sufixo que o HTML acrescenta ao nome nos cards das seções "prioridade"
# (Resfriados), pra diferenciar de um produto de mesmo nome numa seção
# normal (ex: "frango inteiro" resfriado x "frango inteiro" congelado).
SUFIXO_POR_DATA_SEC = {
    "resfBdj": " resfriado bandeja",
    "resfInd": " resfriado individual",
}

# ── Nome de exibição fixo por (seção, nome normalizado) - mesma ideia do
# NOME_CANONICO do estoque, mas pra Tabela de Preços. Preenchido pelo
# painel "Renomear" (que gera o trecho pronto pra colar aqui) ou à mão.
# É reaplicado em TODO card já existente a cada atualização (não só nos
# recém-criados), então uma entrada nova aqui corrige o nome mesmo de um
# card que já existia com outro nome.
NOME_CANONICO_PRECO = {
    # "peito|filezinho sassami ind ou 1kg": "Filezinho Sassami Ind. ou Pct 1kg",
}


def carregar_precos(csv_text):
    """Lê a aba Preços (NOME, REFERENCIA, PRECO_1, PRECO_2, PRECO_3, Seção
    (referência)) e devolve { (data_sec, nome_normalizado): [preco1, preco2,
    preco3] }, com None nas posições vazias (posição vazia = não mexe no
    valor que já está no site).

    A chave inclui o data_sec (derivado da coluna "Seção (referência)")
    porque o mesmo NOME pode aparecer em mais de uma seção da planilha com
    preços diferentes (ex: "filé de peito" existe resfriado em bandeja,
    resfriado individual, E congelado normal - são 3 produtos/preços
    distintos que homônimos)."""
    reader = csv.reader(StringIO(csv_text))
    rows = list(reader)
    precos = {}
    for r in rows[2:]:
        if not r or not r[0]:
            continue
        nome = r[0].strip()
        secao_ref = r[5].strip() if len(r) > 5 else ""
        data_sec = SECAO_REF_PARA_DATA_SEC.get(_normalizar_nome_preco(secao_ref))
        if not data_sec:
            print(
                f"AVISO - preço de '{nome}': Seção (referência) '{secao_ref}' "
                f"não reconhecida - linha ignorada (não sei em qual card aplicar)."
            )
            continue

        valores = []
        for i in (2, 3, 4):  # colunas PRECO_1, PRECO_2, PRECO_3
            bruto = r[i].strip() if len(r) > i else ""
            valores.append(fmt_preco(bruto))

        chave = (data_sec, _normalizar_nome_preco(nome))
        if chave in precos:
            print(
                f"AVISO - preço de '{nome}' (seção '{secao_ref}') está "
                f"duplicado na planilha - usando a última linha encontrada."
            )
        precos[chave] = {"nome": nome, "valores": valores}
    return precos


# ── Como montar um card de preço novo, por data_sec ──
# Diferente do card de estoque (onde a seção precisa ser "adivinhada" pela
# descrição), aqui a própria planilha já diz a seção via "Seção
# (referência)" - então um produto novo na Tabela de Preços pode ser
# colocado direto no lugar certo, sem incerteza nenhuma.
TEMPLATE_CARD_PRECO_POR_DATA_SEC = {
    "resfBdj": {"classe_card": "card-2 priority-card", "classe_nome": "name priority-name",
                "sufixos_preco": [" main green", ""], "sufixo_nome": " resfriado bandeja"},
    "resfInd": {"classe_card": "card-1 priority-card", "classe_nome": "name priority-name",
                "sufixos_preco": [" main green"], "sufixo_nome": " resfriado individual"},
    "copa": {"classe_card": "card-2 copa-card", "classe_nome": "name copa-name",
             "sufixos_preco": [" main copa-price", ""], "sufixo_nome": ""},
}
TEMPLATE_CARD_PRECO_PADRAO = {
    "classe_card": "card", "classe_nome": "name",
    "sufixos_preco": [" main", "", ""], "sufixo_nome": "",
}


def _chave_nome_canonico_preco(data_sec, nome_normalizado):
    return f"{data_sec}|{nome_normalizado}"


def montar_card_preco_novo_html(data_sec, nome_original, valores):
    """Monta o HTML de um card de preço novo, no formato certo pra seção
    (bandeja/individual/normal/copa), igual ao que o script faz pros cards
    de estoque em montar_card_novo_html(). O nome exibido prioriza
    NOME_CANONICO_PRECO (fixado manualmente via o painel "Renomear"); só usa
    o nome bruto da planilha, formatado, quando não está mapeado."""
    template = TEMPLATE_CARD_PRECO_POR_DATA_SEC.get(data_sec, TEMPLATE_CARD_PRECO_PADRAO)
    chave_canonico = _chave_nome_canonico_preco(data_sec, _normalizar_nome_preco(nome_original))
    nome_exibicao = NOME_CANONICO_PRECO.get(chave_canonico) or slug_nome(nome_original)
    nome_exibicao_esc = nome_exibicao.replace('"', "&quot;")
    data_name = (nome_original.strip().lower() + template["sufixo_nome"]).replace('"', "&quot;")

    spans_html = "".join(
        f'<span class="p{suf}">{valores[i]}</span>'
        for i, suf in enumerate(template["sufixos_preco"])
        if i < len(valores) and valores[i]
    )
    # "new-item" marca o card como recém-criado, pro painel destacar ele e
    # oferecer o botão de renomear (persiste nas próximas atualizações,
    # porque atualizar_precos_html nunca reescreve a lista de classes do
    # card - só troca os valores dentro dos <span class="p...">).
    return (
        f'<div class="{template["classe_card"]} new-item" data-sec="{data_sec}" data-name="{data_name}">'
        f'<span class="{template["classe_nome"]}">{nome_exibicao_esc}</span>'
        f'{spans_html}</div>'
    ), data_name


def inserir_cards_preco_novos(html, precos, nao_usados, trend):
    """Cria um card pra cada linha de preço da planilha que não bateu com
    nenhum card existente (nao_usados) - inserido no final da seção certa
    (data_sec), igual ao inserir_cards_novos() faz no painel de estoque.
    Marca a tendência do card novo como 'new'. Devolve o HTML atualizado."""
    inseridos = []
    for chave in nao_usados:
        data_sec, _ = chave
        info = precos[chave]
        card_html, data_name = montar_card_preco_novo_html(data_sec, info["nome"], info["valores"])

        secao_re = re.compile(r'<div class="section[^"]*" data-sec="' + re.escape(data_sec) + r'">')
        m_secao = secao_re.search(html)
        if not m_secao:
            print(f"AVISO - preço de '{info['nome']}': seção '{data_sec}' não existe no HTML - card NÃO foi criado.")
            continue
        secao_end = _find_matching_div_end(html, m_secao.start())
        if secao_end == -1:
            print(f"AVISO - preço de '{info['nome']}': não consegui delimitar a seção '{data_sec}' no HTML - card NÃO foi criado.")
            continue

        bloco_secao = html[m_secao.start():secao_end]
        idx_body = bloco_secao.find('<div class="section-body">')
        if idx_body == -1:
            print(f"AVISO - preço de '{info['nome']}': não achei o corpo da seção '{data_sec}' - card NÃO foi criado.")
            continue
        body_end = _find_matching_div_end(bloco_secao, idx_body)
        if body_end == -1:
            print(f"AVISO - preço de '{info['nome']}': não consegui delimitar o corpo da seção '{data_sec}' - card NÃO foi criado.")
            continue

        ponto_insercao = body_end - len("</div>")
        novo_bloco_secao = bloco_secao[:ponto_insercao] + card_html + bloco_secao[ponto_insercao:]
        html = html[: m_secao.start()] + novo_bloco_secao + html[secao_end:]

        trend[data_name] = "new"
        inseridos.append(info["nome"])

    if inseridos:
        print(f"OK - {len(inseridos)} card(s) de preço novo(s) criado(s) automaticamente: {', '.join(inseridos)}")

    return html


def atualizar_precos_html(html, precos):
    card_pattern = re.compile(
        r'(<div class="card[^"]*" data-sec="([^"]*)" data-name="([^"]+)">)(.*?)(</div>)', re.S
    )

    trend_m = re.search(r"var PRICE_TREND = (\{.*?\})\s*;", html, re.S)
    try:
        trend = json.loads(trend_m.group(1)) if trend_m else {}
    except json.JSONDecodeError:
        trend = {}

    # índice auxiliar por data_sec, pra permitir "match por prefixo" quando o
    # nome da planilha é uma versão abreviada do nome do card (ex: planilha
    # tem "fut wings trad (coxinha emp)", card tem "fut wings trad coxinha
    # empanada") - sem isso, só bateria em correspondência exatamente igual.
    por_secao = {}
    for (data_sec, nome_norm), info in precos.items():
        por_secao.setdefault(data_sec, []).append((nome_norm, info))

    nomes_usados = set()

    def _achar_info(data_sec, nome_norm_card):
        chave = (data_sec, nome_norm_card)
        if chave in precos:
            nomes_usados.add(chave)
            return precos[chave]
        # fallback 1: prefixo em qualquer direção, só dentro da mesma seção
        candidatos = [
            (n, info) for n, info in por_secao.get(data_sec, [])
            if len(n) >= 6 and (n.startswith(nome_norm_card) or nome_norm_card.startswith(n))
        ]
        if len(candidatos) == 1:
            nomes_usados.add((data_sec, candidatos[0][0]))
            return candidatos[0][1]
        # fallback 2: conjunto de palavras contido no outro, na mesma seção.
        # Cobre o caso real de alguém completar o nome do produto na planilha,
        # inserindo palavra NO MEIO (ex: card "file meio peito 1kg" e planilha
        # "file meio peito indiv ou pct 1kg"): o prefixo não pega isso, e sem
        # esse fallback o script criava um card DUPLICADO do mesmo produto -
        # ficavam dois na tabela, e os reajustes seguintes só entravam em um
        # deles. Só aceita quando existe exatamente UM candidato na seção,
        # pra nunca fundir produtos parecidos mas distintos (ex: "filé de
        # peito" x "filé de peito interfolhado" continuam separados, porque
        # cada um casa exato antes de chegar aqui).
        palavras_card = _palavras_nome_preco(nome_norm_card)
        if len(palavras_card) >= 2:
            candidatos = []
            for n, info in por_secao.get(data_sec, []):
                palavras_planilha = _palavras_nome_preco(n)
                if len(palavras_planilha) < 2:
                    continue
                if palavras_card <= palavras_planilha or palavras_planilha <= palavras_card:
                    candidatos.append((n, info))
            if len(candidatos) == 1:
                print(
                    f"AVISO - preço: card '{nome_norm_card}' (seção {data_sec}) "
                    f"foi pareado com a linha '{candidatos[0][0]}' da planilha "
                    "por semelhança de palavras - o nome na planilha mudou. "
                    "Card reaproveitado em vez de criar um duplicado."
                )
                nomes_usados.add((data_sec, candidatos[0][0]))
                return candidatos[0][1]
        return None

    def processa_card(m):
        abertura, data_sec, nome, inner, fechamento = m.groups()

        nome_essencial = nome
        sufixo = SUFIXO_POR_DATA_SEC.get(data_sec)
        if sufixo and nome_essencial.lower().endswith(sufixo):
            nome_essencial = nome_essencial[: -len(sufixo)]

        # ── Autocorreção: reaplica NOME_CANONICO_PRECO em TODO card, mesmo
        # que o preço dele não tenha batido nesse ciclo - assim uma entrada
        # adicionada depois (via painel "Renomear") corrige o nome de um
        # card que já existia com outro nome, igual o estoque já faz. Só
        # troca o texto exibido - o data-name (usado pra casar com a
        # planilha) fica intocado, então isso nunca quebra o pareamento. ──
        chave_canonico = _chave_nome_canonico_preco(data_sec, _normalizar_nome_preco(nome_essencial))
        nome_canonico = NOME_CANONICO_PRECO.get(chave_canonico)
        if nome_canonico:
            nome_canonico_esc = nome_canonico.replace('"', "&quot;")
            inner = re.sub(
                r'(<span class="name[^"]*">)[^<]*(</span>)',
                lambda mm: mm.group(1) + nome_canonico_esc + mm.group(2),
                inner, count=1,
            )

        info = _achar_info(data_sec, _normalizar_nome_preco(nome_essencial))
        if info is None:
            return abertura + inner + fechamento
        novos_valores = info["valores"]

        spans = list(re.finditer(r'<span class="p([^"]*)">([^<]*)</span>', inner))
        if not spans:
            return m.group(0)

        preco_principal_antigo = spans[0].group(2)
        preco_principal_novo = novos_valores[0] if novos_valores[0] else preco_principal_antigo

        novo_inner = inner
        # aplica de trás pra frente pra não bagunçar os índices das outras substituições
        for i in reversed(range(len(spans))):
            if i >= len(novos_valores) or novos_valores[i] is None:
                continue
            span = spans[i]
            classes = span.group(1)
            novo_span = f'<span class="p{classes}">{novos_valores[i]}</span>'
            novo_inner = novo_inner[: span.start()] + novo_span + novo_inner[span.end():]

        # tendência: compara o preço principal (primeira coluna) antigo vs novo
        # (chave = data-name COMPLETO do card, não o nome da planilha - assim
        # dois cards homônimos em seções diferentes têm tendências separadas)
        try:
            antigo_f = float(preco_principal_antigo.replace(".", "").replace(",", "."))
            novo_f = float(preco_principal_novo.replace(".", "").replace(",", "."))
            if nome not in trend:
                trend[nome] = "new"
            elif novo_f > antigo_f:
                trend[nome] = "up"
            elif novo_f < antigo_f:
                trend[nome] = "down"
            else:
                trend[nome] = "same"
        except ValueError:
            pass

        return abertura + novo_inner + fechamento

    new_html = card_pattern.sub(processa_card, html, count=0)

    nao_usados = set(precos.keys()) - nomes_usados
    if nao_usados:
        print(f"AVISO - {len(nao_usados)} linha(s) de preço da planilha não bateram com nenhum card do HTML - tentando criar card novo pra cada uma:")
        for data_sec, nome_norm in sorted(nao_usados):
            print(f"  - seção '{data_sec}': '{nome_norm}'")
        # A "Seção (referência)" da planilha já diz exatamente onde o produto
        # deve entrar - diferente do estoque, aqui não tem adivinhação por
        # palavra-chave: se a seção existe no HTML, o card é criado ali.
        new_html = inserir_cards_preco_novos(new_html, precos, nao_usados, trend)

    new_trend_str = json.dumps(trend, ensure_ascii=False)
    new_html, n_subs = re.subn(
        r"var PRICE_TREND = \{.*?\}\s*;",
        "var PRICE_TREND = " + new_trend_str.replace("\\", "\\\\") + ";",
        new_html, count=1, flags=re.S,
    )

    # ── Validade da tabela: sempre a próxima sexta-feira a partir de hoje ──
    # (se hoje já for sexta, mantém hoje - a tabela ainda vale até o fim do
    # dia). Isso substitui a data fixa no badge toda vez que os preços são
    # atualizados, então o aviso de "tabela vencida" nunca mais fica parado
    # numa data antiga.
    hoje_brt = (datetime.now(timezone.utc) - timedelta(hours=3)).date()
    dias_ate_sexta = (4 - hoje_brt.weekday()) % 7  # segunda=0 ... sexta=4
    proxima_sexta = hoje_brt + timedelta(days=dias_ate_sexta)
    nova_validade = proxima_sexta.strftime("%d/%m/%Y")
    new_html = re.sub(
        r'(<div class="validity-badge">📅 Válida até )\d{2}/\d{2}/\d{4}(</div>)',
        rf"\g<1>{nova_validade}\g<2>",
        new_html,
    )

    return new_html


def carregar_pedidos(csv_text):
    """Lê a planilha bruta de pedidos (mesmas colunas do relatório do ERP:
    Lj Embarque, Cod. Vendedor, Vendedor, Pedido, Data Pedido, Liberado,
    Cliente/Loja, Razao Social, Bairro, CEP, Cidade, UF, Obs. Pedido,
    Dt Carregamento, Dt Entrega, Cod. Produto, Produto, Qtd Liberada,
    Qtd Liberada Total, Peso Cx, Vl Total Pedido, Viagem, Carga,
    Gestão de Pedidos, Saldo sem Gestão) e agrupa em
    { vendedor: { cod_vendedor, pedidos: [ {..., produtos: [...]} ] } }
    (sem campo de senha - isso é preservado à parte, do HTML já existente)."""
    reader = csv.reader(StringIO(csv_text))
    rows = list(reader)

    vendedores = {}
    pedidos_por_vendedor = {}

    for r in rows[2:]:
        if not r or not r[0] or len(r) < 25:
            continue
        (lj_embarque, cod_vendedor, vendedor, pedido, data_pedido, liberado,
         cliente_loja, razao_social, bairro, cep, cidade, uf, obs_pedido,
         dt_carregamento, dt_entrega, cod_produto, nome_produto, qtd_liberada,
         qtd_liberada_total, peso_cx, vl_total_pedido, viagem, carga,
         gestao_pedidos, saldo_sem_gestao) = r[:25]

        if not vendedor:
            continue
        vendedores.setdefault(vendedor, cod_vendedor)
        chave_pedido = (vendedor, pedido)
        lista = pedidos_por_vendedor.setdefault(vendedor, {})
        if chave_pedido not in lista:
            lista[chave_pedido] = {
                "pedido": pedido, "lj_embarque": lj_embarque, "data_pedido": data_pedido,
                "liberado": liberado, "cliente_loja": cliente_loja, "razao_social": razao_social,
                "bairro": bairro, "cep": cep, "cidade": cidade, "uf": uf,
                "obs_pedido": obs_pedido, "dt_carregamento": dt_carregamento,
                "dt_entrega": dt_entrega, "vl_total_pedido": vl_total_pedido,
                "viagem": viagem, "carga": carga, "gestao_pedidos": gestao_pedidos,
                "produtos": [],
            }
        if cod_produto:
            try:
                ql = float(qtd_liberada.replace(".", "").replace(",", ".")) if qtd_liberada else 0
                qlt = float(qtd_liberada_total.replace(".", "").replace(",", ".")) if qtd_liberada_total else 0
            except ValueError:
                ql = qlt = 0
            saldo_norm = saldo_sem_gestao
            if saldo_sem_gestao:
                try:
                    saldo_val = float(str(saldo_sem_gestao).replace(".", "").replace(",", "."))
                    saldo_norm = str(int(saldo_val)) if saldo_val == int(saldo_val) else str(saldo_val).replace(".", ",")
                except ValueError:
                    saldo_norm = saldo_sem_gestao

            produtos = lista[chave_pedido]["produtos"]
            existente = next((p for p in produtos if p["cod"] == cod_produto), None)
            if existente is None:
                produtos.append({
                    "cod": cod_produto, "nome": nome_produto,
                    "qtd_liberada": ql, "qtd_liberada_total": qlt,
                    "peso_cx": peso_cx, "saldo_sem_gestao": saldo_norm,
                    "_ql_vistos": {ql},
                })
            else:
                # Mesmo produto já apareceu neste pedido:
                # - se essa qtd_liberada já foi vista antes, é linha duplicada
                #   (erro de exportação do ERP) -> ignora, não soma de novo
                # - se é uma qtd_liberada nova, é uma liberação parcial
                #   adicional -> soma na quantidade liberada exibida
                #   (qtd_liberada_total NÃO muda, é o mesmo total do pedido)
                if ql not in existente["_ql_vistos"]:
                    existente["_ql_vistos"].add(ql)
                    existente["qtd_liberada"] += ql

    resultado = {}
    for vendedor, cod_vendedor in vendedores.items():
        pedidos_lista = list(pedidos_por_vendedor[vendedor].values())
        for p in pedidos_lista:
            for prod in p["produtos"]:
                prod.pop("_ql_vistos", None)
        resultado[vendedor] = {
            "cod_vendedor": cod_vendedor,
            "pedidos": pedidos_lista,
        }
    return resultado


# ── Senha inicial por vendedor - preenchida pelo painel "definir senha"
# (que gera o trecho pronto pra colar aqui) ou à mão. Só é usada enquanto a
# senha do vendedor no site continuar sendo o padrão "0000" - assim que
# alguém define uma senha de verdade (por aqui ou direto no painel), ela
# gruda e o script para de mexer nela, mesmo que essa entrada seja removida
# depois.
SENHAS_INICIAIS = {
    # "VENDEDOR TESTE NOVO": "1234",
}


# ── Quem saiu do quadro. O painel de pedidos guarda os vendedores antigos
# pra não perder senha nem histórico, então sair da planilha não basta: sem
# esta lista, quem foi desligado fica pra sempre no site (e volta se um
# pedido antigo dele reaparecer na planilha). É só escrever o nome exato
# como aparece na coluna "Vendedor".
VENDEDORES_REMOVIDOS = {
    "RODRIGO GLAUBER CORREA",
}


def _vendedor_removido(nome):
    return str(nome).strip().upper() in {v.strip().upper() for v in VENDEDORES_REMOVIDOS}


def _vendedor_fantasma(nome, info):
    """True quando a entrada do DATA é, na verdade, a linha de cabeçalho da
    planilha que um dia entrou como se fosse um vendedor (nome "Vendedor",
    código "Cod. Vendedor"). Sem isso ela sobrevive pra sempre, porque o
    laço que preserva vendedores antigos copia tudo que já está publicado."""
    rotulos = {"vendedor", "cod. vendedor", "cod vendedor", "codigo do vendedor"}
    cod = str((info or {}).get("cod_vendedor", "")).strip().lower()
    return str(nome).strip().lower() in rotulos or cod in rotulos


def atualizar_pedidos_html(html, novos_dados):
    """Reconstrói o DATA do painel de pedidos, preservando as senhas já
    cadastradas no arquivo (a planilha não tem senha, isso é fixo no HTML).
    Quem entrou agora nasce com a senha padrão "0000" até alguém cadastrar a
    dele (em SENHAS_INICIAIS aqui embaixo, ou direto no DATA do HTML). O
    painel não marca mais nada como "vendedor novo" - o selo foi removido a
    pedido; pra saber quem ainda está sem senha, o log da rodada avisa."""
    m = re.search(r"const DATA = (\{.*?\});", html, re.S)
    if not m:
        return html
    data_atual = json.loads(m.group(1))

    novo_data = {}
    sem_senha = []
    for vendedor, info in novos_dados.items():
        if _vendedor_removido(vendedor):
            print(f"AVISO - pedidos: '{vendedor}' está em VENDEDORES_REMOVIDOS "
                  f"(saiu do quadro) - fora do painel, com {len(info['pedidos'])} pedido(s).")
            continue
        senha_atual = data_atual.get(vendedor, {}).get("senha", "0000")
        # SENHAS_INICIAIS só "vence" enquanto a senha no site ainda for o
        # padrão "0000" - depois que alguém define uma senha real, ela é
        # definitiva (o script nunca mais sobrescreve por conta própria).
        senha_final = senha_atual if senha_atual != "0000" else SENHAS_INICIAIS.get(vendedor, senha_atual)
        if senha_final == "0000":
            sem_senha.append(vendedor)
        novo_data[vendedor] = {
            "cod_vendedor": info["cod_vendedor"],
            "senha": senha_final,
            "pedidos": info["pedidos"],
        }
    # mantém vendedores antigos que não vieram na planilha nova (sem pedidos novos)
    for vendedor, info in data_atual.items():
        if _vendedor_fantasma(vendedor, info):
            print(f"AVISO - pedidos: descartando '{vendedor}' (cód. "
                  f"{info.get('cod_vendedor')!r}) - é linha de cabeçalho, não vendedor.")
            continue
        if _vendedor_removido(vendedor):
            print(f"AVISO - pedidos: '{vendedor}' está em VENDEDORES_REMOVIDOS "
                  "(saiu do quadro) - fora do painel.")
            continue
        if vendedor not in novo_data:
            antigo = {k: v for k, v in info.items() if k != "novo"}
            novo_data[vendedor] = {**antigo, "pedidos": []}

    if sem_senha:
        print("AVISO - pedidos: ainda com a senha padrão 0000 -> "
              + ", ".join(sorted(sem_senha)))

    novo_data_str = json.dumps(novo_data, ensure_ascii=False)
    new_html, n = re.subn(
        r"const DATA = \{.*?\};",
        "const DATA = " + novo_data_str.replace("\\", "\\\\") + ";",
        html, count=1, flags=re.S,
    )
    return new_html


# ══════════════════════════════════════════════════════════════════════
# Conferência antes de publicar
#
# Antes de sobrescrever qualquer HTML, os dados que acabaram de vir da
# planilha passam por uma checagem de sanidade. A ideia é pegar exatamente
# o tipo de problema que já aconteceu antes (ex: secret apontando pra aba
# errada, planilha voltando vazia) ANTES de publicar, em vez de descobrir
# depois pelo painel no ar.
#
# - "Erro" = a planilha claramente não está confiável (vazia, ou perdeu
#   mais da metade dos itens de uma hora pra outra). Nesse caso o arquivo
#   daquela página NÃO é sobrescrito - o site continua com a última versão
#   boa - e a rodada termina marcada como falha no GitHub Actions (fica
#   vermelha na aba Actions), pra dar pra perceber que algo está errado.
# - "Aviso" = algo que vale a pena olhar (preço com salto grande, valor
#   inválido pontual) mas não impede a publicação, porque pode ser uma
#   mudança legítima. Aparece no log da rodada mesmo assim.
# ══════════════════════════════════════════════════════════════════════

class ErroValidacao(Exception):
    """Levantada quando os dados de uma planilha não parecem confiáveis o
    bastante pra publicar. Quem chama decide o que fazer (normalmente:
    não escrever o arquivo e seguir pras próximas seções)."""
    def __init__(self, erros):
        self.erros = erros
        super().__init__("; ".join(erros))


# Se sobrar menos que isso da quantidade de itens/produtos/vendedores que já
# estava publicado, é sinal de planilha incompleta ou link errado.
LIMITE_QUEDA = 0.5
# Abaixo desse total anterior, a checagem de queda não faz sentido sozinha
# (poucos itens oscilam bastante por natureza - ex: 2 vendedores virando 1).
MINIMO_PARA_CHECAR_QUEDA = 5
# Só em PEDIDOS: abaixo dessa quantidade de vendedores restantes, a queda
# deixa de ser "movimento normal" e passa a ter cara de planilha cortada -
# aí sim bloqueia a publicação (acima disso é só aviso, ver validar_pedidos).
MINIMO_PLAUSIVEL_PEDIDOS = 3
# Salto de preço maior que isso (pra mais ou pra menos) vira aviso.
LIMITE_SALTO_PRECO = 0.5

# Liga via input manual "forcar_validacao" (Actions → Run workflow) quando
# uma queda grande é real (ex: vendedores concluíram um monte de pedido de
# uma vez) e não planilha incompleta - ignora só a checagem de queda acima,
# nas três seções, só nessa rodada.
FORCAR_VALIDACAO = os.environ.get("FORCAR_VALIDACAO", "").strip().lower() in ("1", "true", "sim")


def _contar_cards_estoque(html):
    return len(re.findall(r'<div class="card[^"]*" data-name="', html))


def validar_estoque(data, html_atual):
    if not data:
        raise ErroValidacao([
            "a planilha de estoque voltou vazia (0 linhas) - "
            "provavelmente o link em GSHEET_CSV_URL está errado ou a "
            "planilha não está mais publicada na web"
        ])
    base = _contar_cards_estoque(html_atual)
    if not FORCAR_VALIDACAO and base >= MINIMO_PARA_CHECAR_QUEDA and len(data) < base * LIMITE_QUEDA:
        raise ErroValidacao([
            f"o número de itens de estoque caiu de {base} pra {len(data)} "
            f"(mais de {int(LIMITE_QUEDA * 100)}%) de uma rodada pra outra - "
            "parece planilha incompleta, não a atualização normal de estoque"
        ])


def _contar_cards_precos(html):
    return len(re.findall(r'<div class="card[^"]*" data-sec="[^"]*" data-name="', html))


def _extrair_precos_atuais(html):
    """{(data_sec, nome_normalizado): preço principal já publicado}, na
    mesma "forma" de chave usada em `precos` (carregar_precos), pra dar pra
    comparar preço novo com preço atual e pegar saltos suspeitos."""
    atuais = {}
    card_pattern = re.compile(
        r'<div class="card[^"]*" data-sec="([^"]*)" data-name="([^"]+)">(.*?)</div>', re.S
    )
    for m in card_pattern.finditer(html):
        data_sec, nome, inner = m.groups()
        nome_essencial = nome
        sufixo = SUFIXO_POR_DATA_SEC.get(data_sec)
        if sufixo and nome_essencial.lower().endswith(sufixo):
            nome_essencial = nome_essencial[: -len(sufixo)]
        spans = re.findall(r'<span class="p[^"]*">([^<]*)</span>', inner)
        if not spans:
            continue
        try:
            valor = float(spans[0].replace(".", "").replace(",", "."))
        except ValueError:
            continue
        atuais[(data_sec, _normalizar_nome_preco(nome_essencial))] = valor
    return atuais


def validar_precos(precos, html_atual):
    """Retorna a lista de avisos (não bloqueiam a publicação). Erros que
    bloqueiam são levantados como ErroValidacao, igual validar_estoque."""
    if not precos:
        raise ErroValidacao([
            "a planilha de preços voltou vazia (0 linhas) - provavelmente "
            "o link em GSHEET_PRECOS_CSV_URL está errado ou aponta pra "
            "aba errada"
        ])

    base = _contar_cards_precos(html_atual)
    if not FORCAR_VALIDACAO and base >= MINIMO_PARA_CHECAR_QUEDA and len(precos) < base * LIMITE_QUEDA:
        raise ErroValidacao([
            f"o número de produtos de preço caiu de {base} pra {len(precos)} "
            f"(mais de {int(LIMITE_QUEDA * 100)}%) de uma rodada pra outra - "
            "parece planilha incompleta ou link apontando pra aba errada"
        ])

    invalidos = []
    for (data_sec, nome_norm), info in precos.items():
        valores = info.get("valores") or []
        if not valores or not valores[0]:
            continue
        try:
            v = float(str(valores[0]).replace(".", "").replace(",", "."))
        except ValueError:
            invalidos.append(info["nome"])
            continue
        if v <= 0:
            invalidos.append(info["nome"])

    avisos = []
    if invalidos:
        pct = len(invalidos) / len(precos)
        msg = (
            f"{len(invalidos)} produto(s) com preço zerado, negativo ou "
            f"não numérico: {', '.join(invalidos[:8])}"
            + ("..." if len(invalidos) > 8 else "")
        )
        if pct > 0.2:
            raise ErroValidacao([msg])
        avisos.append(msg)

    atuais = _extrair_precos_atuais(html_atual)
    saltos = []
    for (data_sec, nome_norm), info in precos.items():
        valores = info.get("valores") or []
        if not valores or not valores[0]:
            continue
        try:
            novo = float(str(valores[0]).replace(".", "").replace(",", "."))
        except ValueError:
            continue
        antigo = atuais.get((data_sec, nome_norm))
        if antigo and antigo > 0 and novo > 0:
            variacao = abs(novo - antigo) / antigo
            if variacao > LIMITE_SALTO_PRECO:
                saltos.append(f"{info['nome']} (R$ {antigo:.2f} → R$ {novo:.2f})")
    if saltos:
        avisos.append(
            f"{len(saltos)} produto(s) com salto de preço maior que "
            f"{int(LIMITE_SALTO_PRECO * 100)}%: " + "; ".join(saltos[:6])
            + ("..." if len(saltos) > 6 else "")
        )

    return avisos


def _contar_vendedores_pedidos(html):
    m = re.search(r"const DATA = (\{.*?\});", html, re.S)
    if not m:
        return 0
    try:
        return len(json.loads(m.group(1)))
    except json.JSONDecodeError:
        return 0


def validar_pedidos(pedidos, html_atual):
    if not pedidos:
        raise ErroValidacao([
            "a planilha de pedidos voltou vazia (0 vendedores) - "
            "provavelmente o link em GSHEET_PEDIDOS_CSV_URL está errado"
        ])

    avisos = []

    # Diferente de estoque e preços (que mudam devagar), a quantidade de
    # vendedores COM PEDIDO EM ABERTO oscila muito por natureza do negócio:
    # basta um dia de faturamento/carregamento forte pra metade dos pedidos
    # sair da planilha de uma vez. Por isso aqui a queda grande NÃO bloqueia
    # a publicação - só vira aviso no log. Bloqueia mesmo só quando o
    # resultado é implausível de verdade (planilha vazia, checado acima, ou
    # sobrou quase nada vindo de uma base bem maior - aí sim tem cara de
    # exportação parcial/planilha cortada no meio).
    base = _contar_vendedores_pedidos(html_atual)
    if not FORCAR_VALIDACAO and base >= MINIMO_PARA_CHECAR_QUEDA and len(pedidos) < base * LIMITE_QUEDA:
        if len(pedidos) < MINIMO_PLAUSIVEL_PEDIDOS:
            raise ErroValidacao([
                f"sobraram só {len(pedidos)} vendedor(es) na planilha de "
                f"pedidos, vindo de {base} - isso é pouco demais pra ser "
                "movimento normal, parece planilha cortada no meio ou "
                "exportação incompleta"
            ])
        avisos.append(
            f"o número de vendedores caiu de {base} pra {len(pedidos)} "
            f"(mais de {int(LIMITE_QUEDA * 100)}%) de uma rodada pra outra - "
            "publiquei assim mesmo (queda grande em pedidos em aberto é "
            "normal quando muita carga fatura de uma vez), mas vale conferir "
            "se a planilha de pedidos está completa"
        )

    invalidos = 0
    for info in pedidos.values():
        for p in info["pedidos"]:
            try:
                float(str(p.get("vl_total_pedido", "0")).replace(".", "").replace(",", "."))
            except ValueError:
                invalidos += 1

    if invalidos:
        avisos.append(f"{invalidos} pedido(s) com valor total inválido (não numérico)")
    return avisos


def main():
    csv_text = baixar_csv(CSV_URL)
    houve_erro_bloqueante = False

    data, linhas_brutas = carregar_saldos(csv_text)

    validades = {}
    if VALIDADES_CSV_URL:
        try:
            validades_csv_text = baixar_csv(VALIDADES_CSV_URL)
            validades = carregar_validades(validades_csv_text)
        except Exception as e:
            # Mesma lógica de programação: uma falha aqui (link expirado,
            # planilha despublicada, erro temporário do Google) não pode
            # travar estoque/preços/pedidos, que não têm nada a ver com
            # validades - só avisa e segue sem o botão "ver validades"
            # atualizado dessa vez.
            print(f"AVISO - falha ao carregar validades: {e}")

    programacao = {}
    nomes_produto_programacao = {}
    if PROGRAMACAO_CSV_URL:
        try:
            programacao_csv_text = baixar_csv(PROGRAMACAO_CSV_URL)
            programacao, nomes_produto_programacao = carregar_programacao_cargas(programacao_csv_text)
        except Exception as e:
            # Não é uma seção com validação própria (não bloqueia o resto) -
            # se a planilha de programação falhar, o estoque segue publicado
            # normalmente, só sem o botão "🚚 programação" atualizado.
            print(f"AVISO - falha ao carregar programação de cargas: {e}")

    with open(REPO_INDEX_PATH, encoding="utf-8") as f:
        html = f.read()

    try:
        validar_estoque(data, html)
        new_html = atualizar_html(html, data, validades, linhas_brutas, programacao, nomes_produto_programacao)
        with open(REPO_INDEX_PATH, "w", encoding="utf-8") as f:
            f.write(new_html)
        print(f"OK - {len(data)} códigos de estoque, {len(validades)} códigos com validade, {len(programacao)} códigos com programação processados.")
    except ErroValidacao as e:
        houve_erro_bloqueante = True
        print("❌ ERRO DE VALIDAÇÃO (estoque) - index.html NÃO foi alterado, publicação cancelada pra esta seção:")
        for erro in e.erros:
            print(f"  - {erro}")

    if PRECOS_CSV_URL:
        try:
            precos_csv_text = baixar_csv(PRECOS_CSV_URL)
            precos = carregar_precos(precos_csv_text)

            with open(REPO_PRECOS_PATH, encoding="utf-8") as f:
                precos_html = f.read()

            avisos = validar_precos(precos, precos_html)
            for aviso in avisos:
                print(f"⚠️ AVISO (preços) - {aviso}")

            novo_precos_html = atualizar_precos_html(precos_html, precos)

            with open(REPO_PRECOS_PATH, "w", encoding="utf-8") as f:
                f.write(novo_precos_html)

            print(f"OK - {len(precos)} produtos de preço processados.")
        except ErroValidacao as e:
            houve_erro_bloqueante = True
            print("❌ ERRO DE VALIDAÇÃO (preços) - tabela-precos.html NÃO foi alterado, publicação cancelada pra esta seção:")
            for erro in e.erros:
                print(f"  - {erro}")
        except Exception as e:
            # Uma falha aqui não deve apagar a atualização de estoque que já
            # rodou com sucesso acima - só avisa e segue (o commit do estoque
            # ainda acontece normalmente).
            print(f"AVISO - falha ao atualizar preços, estoque foi salvo mesmo assim: {e}")

    if PEDIDOS_CSV_URL:
        try:
            pedidos_csv_text = baixar_csv(PEDIDOS_CSV_URL)
            pedidos = carregar_pedidos(pedidos_csv_text)

            with open(REPO_PEDIDOS_PATH, encoding="utf-8") as f:
                pedidos_html = f.read()

            avisos = validar_pedidos(pedidos, pedidos_html)
            for aviso in avisos:
                print(f"⚠️ AVISO (pedidos) - {aviso}")

            novo_pedidos_html = atualizar_pedidos_html(pedidos_html, pedidos)

            with open(REPO_PEDIDOS_PATH, "w", encoding="utf-8") as f:
                f.write(novo_pedidos_html)

            print(f"OK - {len(pedidos)} vendedores / pedidos processados.")
        except ErroValidacao as e:
            houve_erro_bloqueante = True
            print("❌ ERRO DE VALIDAÇÃO (pedidos) - pedidos-em-aberto.html NÃO foi alterado, publicação cancelada pra esta seção:")
            for erro in e.erros:
                print(f"  - {erro}")
        except Exception as e:
            print(f"AVISO - falha ao atualizar pedidos: {e}")

    if houve_erro_bloqueante:
        # Termina com código de erro pra essa rodada aparecer em vermelho na
        # aba Actions do GitHub - mesmo assim, as seções que passaram na
        # checagem já foram escritas em disco acima e ainda vão pro commit
        # normalmente (só a(s) seção(ões) com problema fica(m) como estava(m)).
        print("\n❌ Rodada terminou com pelo menos uma seção reprovada na conferência - dá uma olhada nos erros acima.")
        sys.exit(1)


if __name__ == "__main__":
    main()
